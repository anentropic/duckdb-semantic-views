# Phase 62 Wave 0 — caret rendering coverage for DROP error paths.
#
# Wave 0 stages this fixture as a no-op via `halt`. Plan 04 (Wave 3)
# populates the `statement error` blocks once caret rendering is restored.
#
# Behavioural property pinned (from RESEARCH.md §6):
#   B7: caret renders for DROP errors (missing name, etc.)

require semantic_views

statement ok
LOAD semantic_views;

halt

# Phase 62 Plan 04: DROP SEMANTIC VIEW with bad form (missing name) →
# caret on the trailing token; substring "expected" or similar.
