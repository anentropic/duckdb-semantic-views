# Multi-source Semantic Views

One advantage of DuckDB as the host is that is makes it very easy to connect to multiple backends and data sources.

This opens up the possibility of a multi-warehouse semantic view, or a semantic view which blends warehouse tables with local data.

