---
phase: 65.1-phase-65-code-review-remediation
plan: 09
type: execute
wave: 2
depends_on:
  - 65.1-03
files_modified:
  - src/lib.rs
  - src/type_cache.rs
  - src/query/table_function.rs
  - src/ddl/list.rs
  - test/sql/phase651_null_name_inputs.test
autonomous: true
requirements:
  - REM-IN-02
  - REM-IN-03
  - REM-IN-05
  - REM-IN-06

must_haves:
  truths:
    - "`src/type_cache.rs` module deleted; `mod type_cache;` / `pub mod type_cache;` declaration removed from `src/lib.rs`"
    - "`type_id_to_display_name` function in `src/query/table_function.rs` (lines 500-568) deleted; no other references in the crate"
    - "Duplicate `probe_catalog_table_present` in `src/ddl/list.rs:191-210` deleted; the single remaining caller (`sv_list_semantic_views_bind_rust` at list.rs:99) uses `crate::ddl::read_ffi::probe_catalog_table_present`"
    - "Duplicate `write_err` in `src/ddl/list.rs` (around line 216) deleted; callers use `crate::ddl::read_ffi::write_err`"
    - "sqllogictest `phase651_null_name_inputs.test` populated with assertions: `read_yaml_from_semantic_view(NULL) IS NULL`, `get_ddl(NULL, 'x') IS NULL`, `get_ddl('SEMANTIC_VIEW', NULL) IS NULL`"
    - "Crate compiles, all existing tests pass, ~225 LOC purged"
  artifacts:
    - path: src/type_cache.rs
      provides: "DELETED — module no longer exists"
    - path: src/lib.rs
      provides: "Updated module declarations; `mod type_cache` removed"
    - path: src/query/table_function.rs
      provides: "`type_id_to_display_name` function deleted"
    - path: src/ddl/list.rs
      provides: "Duplicate `probe_catalog_table_present` + `write_err` deleted; canonical imports from `read_ffi`"
    - path: test/sql/phase651_null_name_inputs.test
      provides: "Three null-input scalar assertions per D-25"
      contains: "IS NULL"
  key_links:
    - from: src/ddl/list.rs::sv_list_semantic_views_bind_rust
      to: src/ddl/read_ffi.rs::probe_catalog_table_present
      via: "use crate::ddl::read_ffi::probe_catalog_table_present"
      pattern: "read_ffi::probe_catalog_table_present"
---

<objective>
Three independent internal cleanups + one sqllogictest addition, all Rust-side or test-side. No C++ changes.

1. **IN-02 (D-23)** — delete `src/type_cache.rs` entirely + the `mod type_cache;` declaration in `src/lib.rs`.
2. **IN-03 (D-23)** — delete `type_id_to_display_name` function from `src/query/table_function.rs:500-568`.
3. **IN-06 (D-26)** — delete the duplicate `probe_catalog_table_present` and `write_err` from `src/ddl/list.rs` (lines ~191-210 and ~216). Replace single caller in `sv_list_semantic_views_bind_rust` with `crate::ddl::read_ffi::probe_catalog_table_present` and `crate::ddl::read_ffi::write_err`.
4. **IN-05 (D-25)** — populate `test/sql/phase651_null_name_inputs.test` (Plan 01 stub) with three null-input assertions.

~225 LOC purged. No behaviour change. The deletions reduce maintenance surface and confusion (the duplicate `probe_catalog_table_present` is byte-identical to the canonical version).

Wave 2 because it depends on Plan 03 (BorrowedConnection migration also touches `src/ddl/list.rs` — Plan 03 migrates BOTH copies of `probe_catalog_table_present` to accept `&BorrowedConnection`, so this plan's clean-delete of the list.rs copy is conflict-free).

Implements D-23 (IN-02/IN-03), D-25 (IN-05), D-26 (IN-06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/65.1-phase-65-code-review-remediation/65.1-CONTEXT.md
@.planning/phases/65.1-phase-65-code-review-remediation/65.1-RESEARCH.md
@.planning/phases/65.1-phase-65-code-review-remediation/65.1-PATTERNS.md
@src/lib.rs
@src/type_cache.rs
@src/query/table_function.rs
@src/ddl/list.rs
@src/ddl/read_ffi.rs
@test/sql/phase651_null_name_inputs.test
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete type_cache module and type_id_to_display_name function (IN-02 + IN-03)</name>
  <read_first>
    - src/type_cache.rs (full file — confirm no surprising public API consumed elsewhere; if grep shows consumers, halt and surface)
    - src/lib.rs (find the `mod type_cache;` or `pub mod type_cache;` declaration)
    - src/query/table_function.rs lines 500-568 (the `type_id_to_display_name` function to delete)
    - .planning/phases/65.1-phase-65-code-review-remediation/65.1-RESEARCH.md lines 716-723 (D-23 detail)
  </read_first>
  <files>src/lib.rs, src/type_cache.rs, src/query/table_function.rs</files>
  <action>
1. **Search for consumers FIRST** before deleting anything:
   ```
   grep -rn "type_cache" src/ tests/ benches/
   grep -rn "type_id_to_display_name" src/ tests/ benches/
   ```
   Document any non-trivial consumers in the SUMMARY. Per RESEARCH.md, both are unused — IF the grep returns more than the deletion sites themselves, halt and report; do not proceed.

2. **Delete `src/type_cache.rs`** in its entirety (`rm src/type_cache.rs` — or use the Write tool with empty content if that's not supported, then delete via shell).

3. **Remove the `mod type_cache;` (or `pub mod type_cache;`) line from `src/lib.rs`.** If a `use type_cache::*;` or similar import exists, remove that too.

4. **Delete `type_id_to_display_name` from `src/query/table_function.rs`** (lines 500-568 per research). Also delete any helper-only-used-by-this-function private items in the same range. Do NOT delete unrelated items.

5. Verify `cargo build` and `cargo test` both succeed — if anything calls `type_id_to_display_name`, the compile fails and points at the consumer; surface that in the summary rather than papering over with `#[allow(dead_code)]`.
  </action>
  <verify>
    <automated>cargo build &gt; /tmp/claude/p09_t1_build.log 2>&amp;1; RC=$?; tail -40 /tmp/claude/p09_t1_build.log; test $RC -eq 0 &amp;&amp; ! test -f src/type_cache.rs &amp;&amp; ! grep -q "mod type_cache" src/lib.rs &amp;&amp; ! grep -q "type_id_to_display_name" src/query/table_function.rs</automated>
  </verify>
  <acceptance_criteria>
    - `src/type_cache.rs` no longer exists
    - `grep -rn "type_cache" src/` returns zero matches
    - `grep -rn "type_id_to_display_name" src/` returns zero matches
    - `cargo build` exits 0
    - `cargo test` exits 0
    - Approximate LOC delta: -225 (per RESEARCH.md estimate; report actual)
  </acceptance_criteria>
  <done>IN-02 + IN-03 closed: dead code purged; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Dedup probe_catalog_table_present + write_err in src/ddl/list.rs (IN-06)</name>
  <read_first>
    - src/ddl/list.rs (full file — confirm the duplicates at ~191-210 and ~216-... are byte-identical to the canonical versions in `src/ddl/read_ffi.rs`)
    - src/ddl/read_ffi.rs lines 57-95 (the canonical `probe_catalog_table_present` and `write_err`)
    - .planning/phases/65.1-phase-65-code-review-remediation/65.1-RESEARCH.md lines 765-767 (D-26 detail)
    - Note: Plan 03 has already migrated BOTH copies of `probe_catalog_table_present` to accept `&BorrowedConnection`. This plan's job is to delete the list.rs copy and switch the caller to import from `read_ffi`.
  </read_first>
  <files>src/ddl/list.rs</files>
  <action>
1. **Re-verify byte-identical duplication** by diffing the two helper bodies (post-Plan-03 BorrowedConnection migration shape):
   - `src/ddl/list.rs:~191-210` `probe_catalog_table_present` vs `src/ddl/read_ffi.rs:~57-74` canonical
   - `src/ddl/list.rs:~216-...` `write_err` vs `src/ddl/read_ffi.rs:~84-95` canonical
   
   If they have diverged in non-trivial ways post-Plan-03 (e.g. different error messages, different param order), halt and report — do NOT merge silently.

2. **Update the caller** (`sv_list_semantic_views_bind_rust` at `src/ddl/list.rs:~99`) to import the canonical versions:
   ```rust
   use crate::ddl::read_ffi::{probe_catalog_table_present, write_err};
   ```
   (Or convert to fully-qualified `crate::ddl::read_ffi::probe_catalog_table_present` at the call site — whichever matches the existing import style of the file.)

3. **Delete the duplicate definitions** in `src/ddl/list.rs` — the entire `fn probe_catalog_table_present(...)` block (~191-210) and the entire `fn write_err(...)` block (~216-... — find the end via brace matching).

4. Verify all other usages in list.rs (e.g. the second dispatcher `sv_list_terse_semantic_views_bind_rust` at line 265) also work with the imported canonical versions. If they were using the local copies, they automatically pick up the imports — verify by `cargo build`.

5. Check `cargo test` for `src/ddl/list.rs` integration tests — they should pass unchanged.
  </action>
  <verify>
    <automated>cargo build &gt; /tmp/claude/p09_t2_build.log 2>&amp;1; RC=$?; tail -40 /tmp/claude/p09_t2_build.log; test $RC -eq 0 &amp;&amp; cargo test &gt; /tmp/claude/p09_t2_test.log 2>&amp;1; RC2=$?; tail -40 /tmp/claude/p09_t2_test.log; test $RC2 -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `src/ddl/list.rs` contains zero `fn probe_catalog_table_present` definitions (only imports/usages)
    - `src/ddl/list.rs` contains zero `fn write_err` definitions (only imports/usages)
    - Both `sv_list_semantic_views_bind_rust` and `sv_list_terse_semantic_views_bind_rust` call the canonical versions from `read_ffi`
    - `cargo build` and `cargo test` exit 0
  </acceptance_criteria>
  <done>IN-06 closed: duplicates removed; canonical imports in place.</done>
</task>

<task type="auto">
  <name>Task 3: Populate phase651_null_name_inputs.test sqllogictest (IN-05)</name>
  <read_first>
    - test/sql/phase651_null_name_inputs.test (the Plan 01 stub)
    - test/sql/65_metadata_via_sql.test (template for existing scalar null-handling assertions to copy the style)
    - .planning/phases/65.1-phase-65-code-review-remediation/65.1-CONTEXT.md (D-25 — exact assertion shapes)
    - .planning/phases/65.1-phase-65-code-review-remediation/65.1-RESEARCH.md lines 745-763 (target test body)
  </read_first>
  <files>test/sql/phase651_null_name_inputs.test</files>
  <action>
Replace the Plan 01 stub body. The populated test must contain at least these three `query I` blocks (D-25 verbatim assertions):

```
# Phase 65.1 Plan 09 (IN-05, D-25) — null name vector input handling for scalar functions

query I
SELECT read_yaml_from_semantic_view(NULL) IS NULL
----
true

query I
SELECT get_ddl(NULL, 'x') IS NULL
----
true

query I
SELECT get_ddl('SEMANTIC_VIEW', NULL) IS NULL
----
true
```

If any of these scalar functions ALSO accept other null shapes (e.g. `get_ddl(NULL, NULL)`), add those as additional assertions for completeness. But the three above are the D-25 hard requirements.

Confirm the file is in TEST_LIST (Plan 01 added it; verify it's still there).
  </action>
  <verify>
    <automated>just test-sql &gt; /tmp/claude/p09_t3_sql.log 2>&amp;1; RC=$?; tail -40 /tmp/claude/p09_t3_sql.log; test $RC -eq 0 &amp;&amp; grep -q "read_yaml_from_semantic_view(NULL)" test/sql/phase651_null_name_inputs.test &amp;&amp; grep -q "get_ddl(NULL, 'x')" test/sql/phase651_null_name_inputs.test &amp;&amp; grep -q "get_ddl('SEMANTIC_VIEW', NULL)" test/sql/phase651_null_name_inputs.test</automated>
  </verify>
  <acceptance_criteria>
    - File contains all three required `query I` blocks asserting `IS NULL` returns `true`
    - `just test-sql` exits 0 — assertions pass
    - File is still listed in `test/sql/TEST_LIST`
  </acceptance_criteria>
  <done>IN-05 closed: null name vector inputs covered by sqllogictest.</done>
</task>

</tasks>

<verification>
  <automated>just test-all &gt; /tmp/claude/p09_all.log 2>&amp;1; RC=$?; tail -100 /tmp/claude/p09_all.log; test $RC -eq 0</automated>
</verification>

<success_criteria>
- `src/type_cache.rs` deleted; `type_id_to_display_name` deleted; ~225 LOC purged
- `probe_catalog_table_present` and `write_err` duplicates in list.rs deleted
- Single caller in list.rs imports canonical versions from read_ffi
- `phase651_null_name_inputs.test` populated with 3 required assertions
- `just test-all` green
</success_criteria>

<output>
Create `.planning/phases/65.1-phase-65-code-review-remediation/65.1-09-SUMMARY.md` when done.
</output>
