---
phase: 65-overridecontext-connection-teardown
plan: 03
type: execute
wave: 3
depends_on:
  - 65-02
# Plan 03 chooses shape (b) from PATTERNS.md: CatalogReader carries db_handle and opens a
# ConnGuard internally per method. Under shape (b), the 13 read-side bind callbacks in
# src/ddl/*.rs keep calling `extra_info.lookup(...)` / `extra_info.list_all()` unchanged at
# the call site — no per-file change is needed. The 10 ddl/*.rs entries listed under
# `files_audited` are AUDIT-ONLY: Task 3 greps them to assert zero modification was required.
# Actual code modifications are confined to the 5 files in `files_modified`.
files_modified:
  - src/lib.rs
  - src/catalog.rs
  - src/parse.rs
  - src/query/table_function.rs
  - src/query/explain.rs
files_audited:
  - src/ddl/list.rs
  - src/ddl/describe.rs
  - src/ddl/show_columns.rs
  - src/ddl/show_dims.rs
  - src/ddl/show_dims_for_metric.rs
  - src/ddl/show_metrics.rs
  - src/ddl/show_facts.rs
  - src/ddl/show_materializations.rs
  - src/ddl/get_ddl.rs
  - src/ddl/read_yaml.rs
autonomous: true
requirements:
  - LIFE-01
  - LIFE-02
tags:
  - duckdb
  - rust
  - ffi
  - table-function
  - lifecycle

must_haves:
  truths:
    - "`init_extension` no longer calls `duckdb_connect` for either `catalog_conn` (H1) or `query_conn` (H2); both long-lived connections are eliminated"
    - "`CatalogReader` (shape b) carries `db: duckdb_database` instead of `conn: duckdb_connection`; each method internally opens+closes a `ConnGuard`"
    - "`QueryState` carries `db_handle: duckdb_database` + `catalog_table_present: bool`; no cached connection"
    - "`SemanticViewBindData` owns a `ConnGuard` constructed in `bind` and dropped when DuckDB destroys the bind data; `func()` calls `execute_sql_raw(bind_data.conn_guard.raw(), ...)` instead of `state.conn`"
    - "All 13 read-side `register_table_function_with_extra_info` sites still compile and link, with bind callbacks using shape (b) CatalogReader"
    - "Plan 03 modifies code in 5 files only (src/lib.rs, src/catalog.rs, src/parse.rs, src/query/table_function.rs, src/query/explain.rs). The 10 src/ddl/*.rs read-side files are AUDIT-ONLY under shape (b) — extra_info.lookup(...) / extra_info.list_all() call sites need no change because CatalogReader::lookup opens its own ConnGuard internally. A grep + git-diff audit (Task 3 Step 2 + verify) asserts zero per-file modification was required."
    - "Both scalar functions (`GetDdlScalar`, `ReadYamlFromSemanticViewScalar`) continue to work — `type State = CatalogReader;` unchanged at the boundary"
    - "Phase 62 transactional DDL, caret, multi-DB, concurrent, and ADBC tests stay green byte-identical"
    - "B1..B4 + B11 tests in `test/integration/test_readonly_load.py` now PASS (they failed on Plan 01's baseline)"
  artifacts:
    - path: "src/catalog.rs"
      provides: "CatalogReader refactored to shape (b): carries db_handle, opens ConnGuard per method"
      contains: "db: ffi::duckdb_database"
    - path: "src/query/table_function.rs"
      provides: "QueryState + SemanticViewBindData hold db_handle and per-query ConnGuard"
      contains: "conn_guard"
    - path: "src/lib.rs"
      provides: "init_extension no longer owns long-lived duckdb_connection handles"
      contains: "register_table_function_with_extra_info"
  key_links:
    - from: "src/lib.rs::init_extension"
      to: "src/catalog.rs::CatalogReader::new(db_handle, catalog_table_present)"
      via: "extra_info registration"
      pattern: "CatalogReader::new"
    - from: "src/query/table_function.rs::bind"
      to: "src/conn_guard.rs::ConnGuard"
      via: "ConnGuard::open(state.db_handle) stored on SemanticViewBindData"
      pattern: "ConnGuard::open"
---

<objective>
Remove the second long-lived `duckdb_connection` (H2 / `query_conn`) and refactor every read-side table function + 2 scalar
functions to use per-call (or per-query) connections. Choose shape (b) from PATTERNS.md: refactor `CatalogReader` so its
field is `db: duckdb_database` and each method internally opens/closes a `ConnGuard`. Wire `QueryState` to carry `db_handle`
+ `catalog_table_present`; `SemanticViewBindData` owns the per-query `ConnGuard` so `execute_sql_raw` keeps using one stable
connection per query.

Purpose: After Plan 02 removed H1 from the parser_override path, H2 is the remaining `shared_ptr<DatabaseInstance>` keeper.
The 14 read-side bind callbacks and 2 scalars also hold copies of the H1 pointer and must transition off it. This is the
rewiring plan; after it lands the in-process RO reopen returns in <5s and the B1..B4 + B11 tests flip green.

Output: Zero long-lived extension-owned `duckdb_connection` handles; LIFE-01 satisfied.
</objective>

<execution_context>
@/Users/paul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-overridecontext-connection-teardown/65-CONTEXT.md
@.planning/phases/65-overridecontext-connection-teardown/65-RESEARCH.md
@.planning/phases/65-overridecontext-connection-teardown/65-PATTERNS.md
@.planning/phases/65-overridecontext-connection-teardown/65-VALIDATION.md
@.planning/phases/65-overridecontext-connection-teardown/65-02-SUMMARY.md
@CLAUDE.md

<interfaces>
Current CatalogReader (src/catalog.rs:97-124) — being refactored to shape (b):
- struct CatalogReader { conn: ffi::duckdb_connection, catalog_table_present: bool } with derive(Clone, Copy)
- methods: new(conn, present), raw() -> conn, lookup, list_all, list_names, exists

Target shape (b):
- struct CatalogReader { db: ffi::duckdb_database, catalog_table_present: bool }
- new(db, present); raw() REMOVED
- each method opens `ConnGuard::open(self.db)?` then calls the existing free-function helper (`prepared_lookup`, `execute_list_all`, etc.) with `guard.raw()`

Current QueryState (src/query/table_function.rs:32-43):
- struct QueryState { catalog: CatalogReader, conn: ffi::duckdb_connection } with derive(Clone)
- unsafe impl Send/Sync for QueryState

Target QueryState:
- struct QueryState { db_handle: ffi::duckdb_database, catalog_table_present: bool } with derive(Clone, Copy)
- unsafe impl Send/Sync preserved

13 read-side register sites (src/lib.rs:421-479):
- ListSemanticViewsVTab, ListTerseSemanticViewsVTab, ShowColumnsInSemanticViewVTab, DescribeSemanticViewVTab,
  ShowSemanticDimensionsVTab, ShowSemanticDimensionsAllVTab, ShowDimensionsForMetricVTab,
  ShowSemanticMetricsVTab, ShowSemanticMetricsAllVTab, ShowSemanticFactsVTab, ShowSemanticFactsAllVTab,
  ShowSemanticMaterializationsVTab, ShowSemanticMaterializationsAllVTab

2 scalar registrations (src/lib.rs:485-490):
- GetDdlScalar, ReadYamlFromSemanticViewScalar — both `type State = CatalogReader;`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor `CatalogReader` to shape (b) (field swap + per-method ConnGuard)</name>
  <files>
    src/catalog.rs
    src/parse.rs
  </files>
  <read_first>
    src/catalog.rs
    src/parse.rs
    src/conn_guard.rs
    .planning/phases/65-overridecontext-connection-teardown/65-PATTERNS.md
  </read_first>
  <behavior>
    - `CatalogReader` field changes from `conn: ffi::duckdb_connection` to `db: ffi::duckdb_database`.
    - Constructor signature becomes `pub fn new(db: ffi::duckdb_database, catalog_table_present: bool) -> Self`.
    - `lookup`, `list_all`, `list_names`, `exists` each open a fresh `ConnGuard` from `self.db` at the top of the method,
      run their existing helper-function logic against `guard.raw()`, and the guard drops at scope end.
    - `raw()` accessor is removed.
    - `Clone + Copy` derives preserved.
    - `Send + Sync` impls preserved with updated SAFETY comments.
    - Internal helpers (`prepared_lookup`, `execute_list_all`, `execute_list_names`, etc.) continue to accept `conn` and
      remain unchanged.
    - Plan 02's transient `CatalogReader::new(guard.raw(), true)` constructions inside `parse.rs` become
      `CatalogReader::new(ctx.db_handle, ctx.catalog_table_present)`. The locally-held `guard` in parse.rs may still be
      retained for direct raw-API calls (`execute_sql_raw(guard.raw(), ...)`) in the same scope.
  </behavior>
  <action>
    Step 1 — Edit `src/catalog.rs`:
    - Replace the `conn: ffi::duckdb_connection` field on `CatalogReader` with `db: ffi::duckdb_database`. Keep
      `catalog_table_present: bool` and `#[derive(Clone, Copy)]`.
    - Update `CatalogReader::new` signature to `pub fn new(db: ffi::duckdb_database, catalog_table_present: bool) -> Self`.
    - Remove `pub fn raw(&self) -> ffi::duckdb_connection`.
    - For each public method (`lookup`, `list_all`, `list_names`, `exists`, plus any others found by
      `grep -E "^\s*pub fn" src/catalog.rs` inside the impl block):
      1. Preserve any short-circuit on `!self.catalog_table_present` if present.
      2. Open `let guard = unsafe { crate::conn_guard::ConnGuard::open(self.db) }.map_err(|e| e)?;` (or with the
         specific error-mapping the method already uses).
      3. Replace the existing `self.conn` usage with `guard.raw()` passed into the existing free-function helper.
    - Update `unsafe impl Send for CatalogReader` / `unsafe impl Sync for CatalogReader` SAFETY comments to reflect the
      "duckdb_database is a non-owning pointer; per-call ConnGuard isolates per-thread reads" rationale.

    Step 2 — Update `src/parse.rs` call sites introduced in Plan 02:
    - Where Plan 02 wrote `CatalogReader::new(guard.raw(), true)`, change to
      `CatalogReader::new(ctx.db_handle, ctx.catalog_table_present)`. The intermediate `guard` is no longer required for
      the `CatalogReader` constructor (the reader opens its own connection per method), but keep it for any direct
      `execute_sql_raw(guard.raw(), ...)` calls in the same scope (the enrichment block in `src/parse.rs:1989-2035`).
    - Audit the enrichment block: if it issues multiple raw-API reads in a tight loop, prefer one outer `ConnGuard` whose
      `guard.raw()` is passed to a per-iteration helper rather than letting `CatalogReader` open one per method call.
      Per-call cost is ~µs (RESEARCH §6.5) so this is a readability call; planner discretion.

    Step 3 — Update existing `#[cfg(not(feature = "extension"))]` unit tests in `src/catalog.rs::tests`:
    - These tests construct a `CatalogReader` and need to pass a `duckdb_database` pointer. Two viable approaches:
      (i) Use `duckdb::Connection::open_in_memory()` (bundled), then extract the underlying `duckdb_database` if the
          duckdb-rs binding exposes an accessor (depends on Spike A6's outcome — see Plan 01 SUMMARY).
      (ii) If the bundled binding does not expose `duckdb_database`, leave the existing tests targeting the free-function
           helpers directly (`prepared_lookup(conn, ...)`, etc.) — they remain unchanged.
    - Add a minimal new test (`#[test] fn catalog_reader_absent_short_circuits` or similar) that constructs a
      `CatalogReader::new(std::ptr::null_mut(), false)` and asserts `lookup`/`list_all` return `Ok(None)` / `Ok(vec![])`
      without touching the FFI (because `catalog_table_present=false` short-circuits before `ConnGuard::open`).

    Step 4 — Build:
    - `cargo build` (bundled) succeeds.
    - `cargo build --features extension --no-default-features` succeeds.
    - `cargo test --lib` (bundled) exits 0.
    - `cargo test --lib --features extension --no-default-features` exits 0.
  </action>
  <verify>
    <automated>cargo build 2>&1 | tail -5 && cargo build --features extension --no-default-features 2>&1 | tail -5 && grep -E "db: (ffi::|libduckdb_sys::)?duckdb_database" src/catalog.rs && ! grep -E "pub fn raw\s*\(&self\)\s*->\s*ffi::duckdb_connection" src/catalog.rs</automated>
  </verify>
  <acceptance_criteria>
    - `CatalogReader` struct's first field is `db: ffi::duckdb_database` (or `libduckdb_sys::duckdb_database`). Verify with
      `grep -E "db: (ffi::|libduckdb_sys::)?duckdb_database" src/catalog.rs` ≥1.
    - Old field gone: `grep -nE "conn: ffi::duckdb_connection" src/catalog.rs` shows no match inside the `struct CatalogReader` block (matches inside other structs like `PreparedStmt` are fine).
    - `raw()` accessor removed: `grep -E "pub fn raw\s*\(&self\)\s*->\s*ffi::duckdb_connection" src/catalog.rs` returns 0 matches.
    - Per-method ConnGuard usage: `grep -c "ConnGuard::open" src/catalog.rs` ≥4 (lookup, list_all, list_names, exists).
    - `cargo build` and `cargo build --features extension --no-default-features` both succeed.
    - `cargo test --lib` exits 0 (no test regressions in bundled mode).
    - `cargo test --lib --features extension --no-default-features` exits 0.
    - `cargo build --features extension` shows zero compile errors in `src/parse.rs` (its Plan 02 call sites now point at the new constructor).
  </acceptance_criteria>
  <done>
    CatalogReader is shape (b). Internal helpers unchanged. Tests pass. parse.rs still compiles against the new constructor.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor `QueryState` + `SemanticViewBindData` for per-query ConnGuard; mirror in `explain_semantic_view`</name>
  <files>
    src/query/table_function.rs
    src/query/explain.rs
  </files>
  <read_first>
    src/query/table_function.rs
    src/query/explain.rs
    src/conn_guard.rs
    src/catalog.rs
    .planning/phases/65-overridecontext-connection-teardown/65-PATTERNS.md
  </read_first>
  <behavior>
    - `QueryState` field set becomes `{ db_handle: duckdb_database, catalog_table_present: bool }`. `Clone + Copy + Send + Sync` retained.
    - In `bind()`, after reading `state` via `get_extra_info`, construct a `ConnGuard` via `ConnGuard::open(state.db_handle)`;
      construct a transient `CatalogReader::new(state.db_handle, state.catalog_table_present)` for the JSON lookup;
      after building the `execution_sql`, MOVE the `ConnGuard` into `SemanticViewBindData` so the connection lives for the
      duration of the query.
    - `SemanticViewBindData` gains a `conn_guard: ConnGuard` field.
    - In `func()`, replace `state.conn` with `bind_data.conn_guard.raw()` for the `execute_sql_raw` call.
    - When DuckDB drops `SemanticViewBindData`, Rust's Drop chain naturally closes the `ConnGuard` → `duckdb_disconnect` fires.
    - `explain_semantic_view` (`src/query/explain.rs`) follows the same pattern; if it does no SQL execution, only the
      bind-side `CatalogReader::new(state.db_handle, ...)` change is needed (no bind-data ConnGuard required).
    - All existing semantic_view query results are identical.
  </behavior>
  <action>
    Step 1 — Edit `src/query/table_function.rs`:

    Replace `QueryState` (currently at `src/query/table_function.rs:32-43`):
    - Remove `pub catalog: CatalogReader` and `pub conn: ffi::duckdb_connection`.
    - Add `pub db_handle: ffi::duckdb_database` and `pub catalog_table_present: bool`.
    - Add `#[derive(Clone, Copy)]` (today only `Clone` is derived; add `Copy` since both fields are Copy).
    - Preserve `unsafe impl Send for QueryState {}` and `unsafe impl Sync for QueryState {}`; update SAFETY comments.

    Add a `conn_guard` field to `SemanticViewBindData` (search for the struct near `src/query/table_function.rs:92`):
    - New field: `pub(crate) conn_guard: crate::conn_guard::ConnGuard,` with a doc comment "Per-query connection. Lives for the duration of bind→func→destroy; Drop closes it."
    - If `SemanticViewBindData` has `#[derive(Clone)]` or similar, remove the Clone derive — `ConnGuard` is intentionally not `Clone`. Any code that previously cloned bind data must be audited and refactored to not clone.

    Update the `bind()` callback (the place that today calls `state.catalog.lookup(&view_name)` — search for that string):
    - Read `state = unsafe { &*bind.get_extra_info::<QueryState>() }` (unchanged).
    - Open `let guard = unsafe { crate::conn_guard::ConnGuard::open(state.db_handle) }.map_err(Box::<dyn std::error::Error>::from)?;`
    - Build `let catalog = crate::catalog::CatalogReader::new(state.db_handle, state.catalog_table_present);`
    - Call `catalog.lookup(&view_name)?` to get the JSON (this internally opens its own short-lived ConnGuard — duplicate of `guard` for ~µs, acceptable).
    - When constructing `SemanticViewBindData`, MOVE `guard` into its `conn_guard` field.

    Update the `func()` callback (the place that today does `execute_sql_raw(state.conn, &bind_data.execution_sql)`):
    - Replace `state.conn` with `bind_data.conn_guard.raw()`.
    - If `state` is no longer referenced in `func()` after this change, remove the `state` lookup entirely.

    Step 2 — Edit `src/query/explain.rs`:
    - Apply the analogous QueryState consumer changes. Most likely `explain_semantic_view` only reads the catalog (no SQL
      execution); in that case it does NOT need a per-query `ConnGuard` stored in bind data — the bind-callback's transient
      `CatalogReader::new(state.db_handle, state.catalog_table_present)` plus the reader's internal per-method ConnGuard
      suffices. Confirm by reading the file before deciding.
    - If `explain_semantic_view` does execute any SQL (e.g., to render a sample of the expansion result), apply the same
      `conn_guard` field on its bind data as Step 1.

    Step 3 — Update any other consumer of `QueryState::catalog` or `QueryState::conn`:
    - `grep -rn "QueryState" src/ | grep -v "//"` and audit each match.
    - `grep -rn "state\.conn\|state\.catalog" src/query/ src/lib.rs` and audit each match.
    - Any remaining `state.conn` reference must transition to a `ConnGuard` (either from bind data or freshly opened).

    Step 4 — Build and sqllogictest spot-check:
    - `cargo build --features extension --no-default-features` succeeds.
    - `just build` succeeds.
    - Run `just test-sql` for any test file that exercises `FROM semantic_view(...)` — e.g.,
      `test/sql/semantic_view_basic.test` or whatever the canonical query-side test is. Exit 0 with no diffs.
    - Save log to `$TMPDIR/65_03_t2_sqllogic.log`.
  </action>
  <verify>
    <automated>cargo build --features extension --no-default-features 2>&1 | tail -10 && grep -E "db_handle: (ffi::|libduckdb_sys::)?duckdb_database" src/query/table_function.rs && ! grep -E "pub conn: ffi::duckdb_connection" src/query/table_function.rs && grep -c "conn_guard:" src/query/table_function.rs</automated>
  </verify>
  <acceptance_criteria>
    - `QueryState` carries `db_handle: duckdb_database` and `catalog_table_present: bool`. Verify with
      `grep -E "db_handle: (ffi::|libduckdb_sys::)?duckdb_database" src/query/table_function.rs` ≥1.
    - Old `pub conn: ffi::duckdb_connection` and `pub catalog: CatalogReader` fields removed from `QueryState`. Verify with
      `grep -E "pub conn: ffi::duckdb_connection|pub catalog: CatalogReader" src/query/table_function.rs` showing 0 matches inside the `struct QueryState` block.
    - `SemanticViewBindData` has a `conn_guard: ConnGuard` field. Verify with `grep -E "conn_guard:.*ConnGuard" src/query/table_function.rs` ≥1.
    - `func()` uses `bind_data.conn_guard.raw()` (not `state.conn`). Verify with
      `grep -E "bind_data\.conn_guard\.raw\(\)|conn_guard\.raw\(\)" src/query/table_function.rs` ≥1 AND
      `grep -E "state\.conn[^_]" src/query/table_function.rs` showing 0 matches.
    - `cargo build --features extension --no-default-features` succeeds.
    - `just build` succeeds.
    - Query-side sqllogictests pass. Evidence path recorded in summary.
  </acceptance_criteria>
  <done>
    H2 ownership has moved out of `QueryState` and into per-query `SemanticViewBindData`. The connection's lifetime is now
    coupled to the query, not the extension. `explain_semantic_view` mirrors the pattern. Query-side regression tests
    stay green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update `init_extension` + 13 read-side bind callbacks + 2 scalars to use shape (b) extra_info</name>
  <files>
    src/lib.rs
    src/ddl/list.rs
    src/ddl/describe.rs
    src/ddl/show_columns.rs
    src/ddl/show_dims.rs
    src/ddl/show_dims_for_metric.rs
    src/ddl/show_metrics.rs
    src/ddl/show_facts.rs
    src/ddl/show_materializations.rs
    src/ddl/get_ddl.rs
    src/ddl/read_yaml.rs
  </files>
  <read_first>
    src/lib.rs
    src/ddl/list.rs
    src/ddl/describe.rs
    src/ddl/show_columns.rs
    src/ddl/show_dims.rs
    src/ddl/show_dims_for_metric.rs
    src/ddl/show_metrics.rs
    src/ddl/show_facts.rs
    src/ddl/show_materializations.rs
    src/ddl/get_ddl.rs
    src/ddl/read_yaml.rs
    .planning/phases/65-overridecontext-connection-teardown/65-PATTERNS.md
  </read_first>
  <behavior>
    - `init_extension` no longer calls `duckdb_connect` to create `catalog_conn` or `query_conn`. The function constructs
      `CatalogReader::new(db_handle, catalog_table_present)` (shape b) directly from `db_handle`, then registers it as
      extra_info for all 13 read-side table functions and both scalars.
    - `init_extension` constructs `QueryState { db_handle, catalog_table_present }` and registers it as extra_info for
      both `SemanticViewVTab` and `ExplainSemanticViewVTab`.
    - The 13 read-side bind callbacks (`src/ddl/*.rs`) need NO change at the call site — they already do
      `bind.get_extra_info::<CatalogReader>()` and call `reader.lookup(...)` / `reader.list_all()` / etc. With shape (b),
      `reader.lookup` etc. now open their own ConnGuard internally — call sites are unchanged.
    - The 2 scalar functions (`GetDdlScalar`, `ReadYamlFromSemanticViewScalar`) similarly have `type State = CatalogReader;`
      and their `invoke` calls `state.lookup(name)` — also unchanged at the call site.
    - The ONLY changes in the 13 + 2 callers are: their construction of `CatalogReader` (via `init_extension`'s registration
      with extra_info) now passes `db_handle` instead of `catalog_conn`. Since `init_extension` is the sole producer of the
      `CatalogReader` passed to extra_info, no per-file change is needed unless a file independently constructed a
      `CatalogReader` (search for `CatalogReader::new(` to find any such).
    - After this task, `grep -E "duckdb_connect\(" src/lib.rs` returns 0 matches in `init_extension`.
    - All pre-existing sqllogictests pass byte-identical.
    - Plan 01's failing B1..B4 + B11 in-process tests NOW PASS (the fix is complete at this point).
  </behavior>
  <action>
    Step 1 — Edit `src/lib.rs::init_extension` (current shape sketched in RESEARCH §5 + PATTERNS.md; lines ~378-517):

    Remove the H1 `catalog_conn` block:
    - Delete:
      ```
      let mut catalog_conn: ffi::duckdb_connection = ptr::null_mut();
      let rc = unsafe { ffi::duckdb_connect(db_handle, &mut catalog_conn) };
      if rc != ffi::DuckDBSuccess { return Err("Failed to create catalog connection".into()); }
      ```
    - Construct `catalog_reader` directly from `db_handle`:
      `let catalog_reader = crate::catalog::CatalogReader::new(db_handle, catalog_table_present);`

    Remove the H2 `query_conn` block:
    - Delete:
      ```
      let mut query_conn: ffi::duckdb_connection = ptr::null_mut();
      let rc = unsafe { ffi::duckdb_connect(db_handle, &mut query_conn) };
      if rc != ffi::DuckDBSuccess { return Err(...); }
      ```
    - Construct `query_state` from `db_handle`:
      `let query_state = QueryState { db_handle, catalog_table_present };`

    The Plan 02 `sv_register_parser_hooks(db_handle, catalog_table_present, is_file_backed)` call is unchanged.

    The 13 `con.register_table_function_with_extra_info::<...VTab, _>(..., &catalog_reader)` calls and the 2
    `con.register_scalar_function_with_state::<...Scalar>(..., &catalog_reader)` calls are syntactically unchanged —
    they pass `&catalog_reader`. Since `CatalogReader` is now shape (b), this just works.

    The `con.register_table_function_with_extra_info::<SemanticViewVTab, _>("semantic_view", &query_state)` and same for
    `explain_semantic_view` calls are syntactically unchanged — they pass `&query_state`. Since `QueryState` is now shape
    (b), this just works.

    Step 2 — Audit the 10 read-side files (`src/ddl/list.rs`, `describe.rs`, `show_columns.rs`, `show_dims.rs`,
    `show_dims_for_metric.rs`, `show_metrics.rs`, `show_facts.rs`, `show_materializations.rs`, `get_ddl.rs`,
    `read_yaml.rs`). These are AUDIT-ONLY under shape (b) — see `files_audited` frontmatter. The audit is codified
    as discrete grep + git-diff gates in this task's `<verify>` block; do NOT edit these files unless an audit gate
    fails (in which case reclassify the file from `files_audited` to `files_modified` and update SUMMARY).

    Audit gates (all must hold):
    1. `grep -l 'CatalogReader::new' src/ddl/*.rs` returns 0 lines — only `src/lib.rs` constructs `CatalogReader`.
       If any ddl/*.rs file constructs one independently, replace `CatalogReader::new(conn, ...)` with
       `CatalogReader::new(db, ...)` and move that file out of `files_audited` into `files_modified`.
    2. `grep -nE '\.raw\(\)|state\.conn' src/ddl/*.rs` returns 0 matches against any `CatalogReader` instance —
       the `raw()` accessor is removed in this plan, and any match needs refactoring to open a `ConnGuard` locally.
    3. `grep -n 'get_extra_info::<CatalogReader>' src/ddl/*.rs` returns ≥10 matches (one per file — confirms the
       call sites are still present and shape-(b)-compatible).
    4. `git diff --numstat src/ddl/` reports zero changed lines across all 10 audit-only files. THIS is the strongest
       assertion that shape (b) genuinely required zero per-file change. If any of the 10 files shows any change,
       STOP and reclassify per gate 1's procedure.

    Step 3 — Build & test:
    - `cargo build` (bundled) succeeds.
    - `cargo build --features extension --no-default-features` succeeds.
    - `just build` succeeds.
    - `just test-sql` exits 0 byte-identical (Phase 62 transactional DDL, all read-side table function tests, caret
      tests, semantic_view query tests, materialization tests, etc.).
    - `uv run test/integration/test_readonly_load.py` — the B1..B4 + B11 tests planted by Plan 01 must now PASS along
      with the pre-existing subprocess tests. Capture log to `$TMPDIR/65_03_t3_readonly.log`.
    - `just test-multi-db` exits 0 (Phase 61 regression).
    - `just test-concurrent` exits 0 (Phase 60 regression).
    - `just test-adbc` exits 0 (Phase 58 regression).
    - `just test-caret` exits 0 (Phase 62 regression).

    Step 4 — Verify no `duckdb_connect` calls remain in `src/lib.rs::init_extension`:
    - `grep -nE "duckdb_connect\(" src/lib.rs` should return 0 matches inside `init_extension` (a match inside an unrelated
      helper or test is acceptable; the only one in init_extension was H1+H2, both deleted).
  </action>
  <verify>
    <automated>just build 2>&1 | tail -5 && uv run test/integration/test_readonly_load.py 2>&1 | tee $TMPDIR/65_03_t3_readonly.log | tail -30; grep -E "(PASS|FAIL|ERROR).*test_in_process|test_repeated_load_close_no_busy_spin" $TMPDIR/65_03_t3_readonly.log; ! grep -nE "duckdb_connect\(" src/lib.rs && [ "$(grep -l 'CatalogReader::new' src/ddl/list.rs src/ddl/describe.rs src/ddl/show_columns.rs src/ddl/show_dims.rs src/ddl/show_dims_for_metric.rs src/ddl/show_metrics.rs src/ddl/show_facts.rs src/ddl/show_materializations.rs src/ddl/get_ddl.rs src/ddl/read_yaml.rs 2>/dev/null | wc -l)" -eq 0 ] && [ "$(git diff --numstat src/ddl/ | awk '{s+=$1+$2} END {print s+0}')" -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "duckdb_connect\(" src/lib.rs` returns 0 matches (both H1 and H2 connects are gone).
    - `grep -E "let catalog_reader = .*CatalogReader::new\(db_handle" src/lib.rs` ≥1 (constructed from db_handle directly).
    - `grep -E "QueryState\s*\{\s*db_handle" src/lib.rs` ≥1.
    - `grep -n "CatalogReader::new" src/ddl/*.rs` returns 0 matches (only lib.rs constructs CatalogReader, or the matches
      are inside tests).
    - `cargo build`, `cargo build --features extension --no-default-features`, and `just build` all succeed.
    - `just test-sql` exits 0 byte-identical (no diffs).
    - `uv run test/integration/test_readonly_load.py` — ALL tests pass, INCLUDING B1..B4 + B11:
      `test_in_process_bootstrap_then_readonly_fresh`, `test_in_process_bootstrap_then_readonly_existing`,
      `test_in_process_load_only_then_readonly`, `test_in_process_readonly_then_readwrite`,
      `test_repeated_load_close_no_busy_spin`. Verify by grepping the log for `FAIL` or `ERROR` returning 0 hits, and
      `PASS` (or equivalent success token) lines for each named test.
    - `just test-multi-db`, `just test-concurrent`, `just test-adbc`, `just test-caret` all exit 0.
  </acceptance_criteria>
  <done>
    Both H1 and H2 long-lived connections are eliminated. CatalogReader and QueryState carry `db_handle`. All 13 read-side
    table functions + 2 scalars work against the new shape with no per-file changes. In-process RO reopen tests pass.
    Phase 62/61/60/58 regression suites stay green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust ↔ DuckDB C-API | Each bind callback now opens+closes its own `duckdb_connection` via `ConnGuard`; per-query connection lifetime now tied to bind data |
| Extension ↔ caller's connection | The extension's connection is short-lived (per query) instead of long-lived (process); behavior at boundary unchanged otherwise |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-07 | Tampering | `SemanticViewBindData` Clone derive — `ConnGuard` is not Clone, so adding the field may break compile if Clone is derived | mitigate | Task 2 explicitly removes `Clone` derive from `SemanticViewBindData` if present; compiler catches any remaining `.clone()` call |
| T-65-08 | Denial of Service | Per-query ConnGuard lifetime — if DuckDB drops bind data before `func()` completes, the connection drops mid-query | accept | DuckDB's BindData lifetime is bind→func→destroy in that order, established convention; structurally safe per duckdb-rs binding |
| T-65-09 | Elevation of Privilege | None — connections still have the same caller-context-derived privileges | accept | n/a |
| T-65-10 | Information Disclosure | None — internal lifecycle change only | accept | n/a |
</threat_model>

<verification>
After all tasks in this plan complete:

1. `cargo build` and `cargo build --features extension --no-default-features` both succeed.
2. `just build` succeeds.
3. `grep -nE "duckdb_connect\(" src/lib.rs` returns 0 matches (init_extension is clean).
4. `grep -E "ConnGuard::open" src/catalog.rs src/parse.rs src/query/table_function.rs src/query/explain.rs` returns matches
   in each file (the per-call open pattern is uniformly applied).
5. `just test-sql` exits 0 byte-identical.
6. `uv run test/integration/test_readonly_load.py` — all tests pass, including the five Plan 01 B1..B4 + B11 tests that
   previously failed on baseline.
7. `just test-multi-db`, `just test-concurrent`, `just test-adbc`, `just test-caret` all exit 0.
8. `git diff --stat src/ cpp/` shows the changes confined to the files listed in `files_modified`.
</verification>

<success_criteria>
- Zero long-lived extension-owned `duckdb_connection` handles. H1 and H2 are both eliminated.
- `CatalogReader` (shape b) carries `db_handle`; per-method ConnGuard open.
- `QueryState` carries `db_handle` + `catalog_table_present`; per-query ConnGuard lives in `SemanticViewBindData`.
- B1..B4 + B11 in-process tests PASS. LIFE-01 satisfied.
- All Phase 58/60/61/62 regression suites stay green byte-identical.
</success_criteria>

<output>
Create `.planning/phases/65-overridecontext-connection-teardown/65-03-SUMMARY.md` when done. Summary MUST include:
- Final `CatalogReader` field list and method-internal ConnGuard pattern.
- Final `QueryState` field list and the `SemanticViewBindData::conn_guard` field placement.
- Confirmation that `grep -nE "duckdb_connect\(" src/lib.rs` returns 0.
- Log path showing all 5 in-process tests (B1-B4, B11) now passing.
- Log path showing Phase 58/60/61/62 regression suites green.
- Any deviation from PATTERNS.md prescribed shape.
</output>
