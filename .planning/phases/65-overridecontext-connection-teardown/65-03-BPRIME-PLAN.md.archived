---
phase: 65-overridecontext-connection-teardown
plan: 03
type: execute
wave: 2
depends_on: [65-02]
files_modified:
  - src/parse.rs
  - cpp/src/shim.cpp
  - cpp/src/shim.hpp
  - src/lib.rs
autonomous: true
requirements:
  - LIFE-01
  - LIFE-02
tags:
  - duckdb
  - cpp
  - ffi
  - parser-extension
  - bprime
  - transactional-ddl

must_haves:
  truths:
    - "sv_parser_override is deregistered — ext.parser_override is no longer assigned in sv_register_parser_hooks; sv_parser_override callback definition removed or marked dead-code with explanatory comment"
    - "sv_parse_function is promoted to the success-path entry — detects CREATE/DROP/ALTER/DESCRIBE/SHOW SEMANTIC VIEW prefixes via the existing match_keyword_prefix helper, runs structural validation via validate_and_rewrite, stashes the validated form + verb + view_name + flags + byte_offset into SemanticViewParseData::payload as a manually LE-encoded byte vector, returns PARSE_SUCCESSFUL"
    - "sv_plan_function is promoted to the catalog-read + native-SQL emission entry. Receives ClientContext &context; opens a per-call C++ `Connection probe(*context.db)`; reinterprets to duckdb_connection; calls into Rust (sv_emit_native_sql_rust) to perform catalog READS (lookup, LIMIT 0 type probes, fact typing) and emit the rewritten SQL string; probe destructs before sv_plan_function returns; returns a ParserExtensionPlanResult whose TableFunction drives the rewritten INSERT/DELETE through the binder onto the CALLER'S conn"
    - "v0.8.0 transactional DDL semantics are preserved byte-identical: catalog writes (INSERT/DELETE on semantic_layer._definitions) ride the binder onto the caller's conn — NOT the per-call probe. Phase 58 transactional DDL tests + ADBC autocommit tests stay green"
    - "Phase 62 caret-rendering tests stay green — sv_parse_function returns DISPLAY_EXTENSION_ERROR with error_location on structural failure, preserving the caret-emission path"
    - "src/lib.rs's local catalog_conn (the `let mut catalog_conn ... duckdb_connect(db_handle, &mut catalog_conn)` block in `init_extension`) is REMOVED — that long-lived H1 connection is gone from init_extension; OverrideContext (still owned by SemanticViewsParserInfo) is migrated back to a non-owning shape with no duckdb_connection or db_handle field — fields { catalog_table_present: bool, is_file_backed: bool } only, or eliminated entirely if no callback reads it (planner-discretion sub-decision at task time)"
    - "just test-sql returns to 47/47 PASS; the B1, B2, B3, B4, B11 watchdog tests from Plan 01 either flip green OR remain red only because read-side H2 still leaks (acceptable mid-phase — Plans 04-06 close H2)"
    - "After Plan 03, `init_extension` body contains ZERO `duckdb_connect` calls for the catalog path. (The H2 `let mut query_conn ... duckdb_connect(...)` open may still survive — Plan 06 retires it. The H1 catalog_conn open is GONE.)"
  artifacts:
    - path: "cpp/src/shim.cpp"
      provides: "Promoted sv_parse_function + sv_plan_function entry points; deregistered sv_parser_override; per-call Connection probe inside sv_plan_function"
      contains: "Connection probe"
    - path: "src/parse.rs"
      provides: "sv_emit_native_sql_rust FFI entry point; SemanticViewParseData payload encoder/decoder helpers; OverrideContext shape under B-prime (no db_handle, no CatalogReader long-lived field)"
      contains: "sv_emit_native_sql_rust"
    - path: "src/lib.rs"
      provides: "init_extension no longer opens catalog_conn (H1 eliminated)"
  key_links:
    - from: "cpp/src/shim.cpp::sv_plan_function"
      to: "Connection probe(*context.db)"
      via: "C++ direct path per 65-OPTION-B-SPIKE.md Probe 1 (PLAN-THREAD-RC0)"
      pattern: "Connection probe"
    - from: "cpp/src/shim.cpp::sv_plan_function"
      to: "ParserExtensionPlanResult driving rewritten SQL through binder onto caller's conn"
      via: "Phase 58 rewrite-to-native pattern preserved verbatim"
      pattern: "ParserExtensionPlanResult"
    - from: "src/parse.rs::sv_emit_native_sql_rust"
      to: "CatalogReader::new(conn, …) on per-call probe conn"
      via: "catalog READS only (writes flow through ParserExtensionPlanResult)"
      pattern: "CatalogReader::new"
---

<objective>
Port the CREATE / DROP / ALTER SEMANTIC VIEW write path from `sv_parser_override` (which is now deregistered) to `sv_parse_function` (structural parse) + `sv_plan_function` (catalog reads + native-SQL emission via per-call `Connection probe(*context.db)`). Preserve v0.8.0 transactional DDL semantics — the catalog write rides the binder onto the caller's conn through the Phase 58 rewrite-to-native pattern, unchanged. Retire the long-lived `catalog_conn` (H1) — the `let mut catalog_conn ... duckdb_connect(db_handle, &mut catalog_conn)` block in `init_extension`. Plan 04 and Plan 05 (read path) can run in parallel after this lands.

Purpose: This is the architectural pivot — moving catalog reads OUT of the parse thread (which empirically fails per D-10) and onto the plan thread where `ClientContext &` is reachable and `Connection(*context.db)` succeeds (PLAN-THREAD-RC0). Transactional DDL non-negotiable per D-20.

Output: Two modified C++ entry points; one new Rust FFI emission helper; OverrideContext stripped to a stateless config holder (or retired entirely if unused); H1 long-lived connection eliminated from init_extension; Phase 58 transactional DDL tests + Phase 62 caret tests stay byte-identical green.
</objective>

<execution_context>
@/Users/paul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/65-overridecontext-connection-teardown/65-CONTEXT.md
@.planning/phases/65-overridecontext-connection-teardown/65-RESEARCH.md
@.planning/phases/65-overridecontext-connection-teardown/65-PATTERNS.md
@.planning/phases/65-overridecontext-connection-teardown/65-OPTION-B-SPIKE.md
@.planning/phases/65-overridecontext-connection-teardown/65-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Canonical B-prime write-path shape — RESEARCH §16.2 + §16.4 verbatim -->

From .planning/phases/65-overridecontext-connection-teardown/65-RESEARCH.md §16.2 (write path):

Step 1: deregister sv_parser_override. In cpp/src/shim.cpp::sv_register_parser_hooks, set `ext.parser_override = nullptr` (or remove the assignment). The default Postgres parser fails on the unrecognised `CREATE SEMANTIC VIEW` prefix and triggers `parse_function` dispatch at the `parse_function` callback site in the bundled `duckdb.cpp` (search for `parse_function(` in the parser-extension dispatch path).

Step 2: sv_parse_function (today is the `sv_parse_stub` function defined in `cpp/src/shim.cpp` — grep `static .* sv_parse_stub(` to locate) becomes the success-path entry:
- Detect supported prefixes via Rust-side match (existing `match_keyword_prefix` helper in `src/parse.rs` — grep `fn match_keyword_prefix` — extend `sv_parse_function_rust` to do this).
- Run structural body validation via `validate_and_rewrite` (existing helper in `src/parse.rs` — grep `fn validate_and_rewrite` or its current call site within the v0.8.0 `sv_parser_override_rust` body).
- Stash validated form into a Rust-side payload: { verb (DdlKind u8), view_name CString, validated_form String, byte_offset u32, flags u8 } encoded LE into a byte vector.
- Hand back via a new C-side carrier on SemanticViewParseData (extend the `SemanticViewParseData` class in `cpp/src/shim.cpp` — grep `class SemanticViewParseData` — to add a `vector<uint8_t> payload` member alongside the existing `string query`).
- Return PARSE_SUCCESSFUL via the existing rc encoding.

Step 3: sv_plan_function (today is `sv_plan_unreachable` in `cpp/src/shim.cpp` — grep `sv_plan_unreachable` — a placeholder body that throws) becomes:

```cpp
static unique_ptr<TableRef> sv_plan_function(ClientContext &context, ParserExtensionInfo *info,
                                              unique_ptr<ParserExtensionParseData> parse_data_ptr) {
    auto *parse_data = static_cast<SemanticViewParseData *>(parse_data_ptr.get());
    string native_sql;
    char *error_msg = nullptr;
    {
        Connection probe(*context.db);
        auto conn = reinterpret_cast<duckdb_connection>(&probe);
        bool ok = sv_emit_native_sql_rust(conn,
            parse_data->payload.data(), parse_data->payload.size(),
            &native_sql_out_buf, &native_sql_out_len, &error_msg);
        // probe destructs here — Connection::~Connection → RemoveConnection
        if (!ok) {
            string msg = error_msg ? string(error_msg) : "plan failed";
            if (error_msg) sv_free_cstring(error_msg);
            throw ParserException(msg);
        }
        native_sql = string(native_sql_out_buf, native_sql_out_len);
        sv_free_cstring(native_sql_out_buf);
    }
    // Build TableRef that drives `native_sql` through the binder onto context's conn.
    // Preserves Phase 58 rewrite-to-native pattern: INSERT/DELETE participates in the
    // caller's BEGIN/COMMIT because it runs on the caller's conn, NOT on `probe`.
    auto select = make_uniq<SelectStatement>();
    /* Parse `native_sql` via context.GetParserExtensions() to produce the select; or
       reuse existing helper that converts rewritten SQL → TableRef. See Phase 58/62 code
       in shim.cpp for the exact constructor used by today's sv_parser_override path. */
    return /* TableRef for native_sql */;
}
```

NB: the existing Phase 58/62 parser_override path already builds a ParserExtensionPlanResult / TableRef from native_sql. Reuse the EXACT same construction code — only the source of the catalog reads changes (per-call probe instead of long-lived catalog_conn).

Step 4: SemanticViewParseData payload encoder/decoder (new Rust helpers in src/parse.rs):

```rust
// Manual LE encoding — no new deps. Format:
//   [verb: u8][flags: u8][byte_offset: u32 LE][view_name_len: u32 LE][view_name: bytes]
//   [validated_form_len: u32 LE][validated_form: bytes]
fn encode_parse_payload(verb: DdlKind, flags: u8, byte_offset: u32,
                        view_name: &str, validated_form: &str) -> Vec<u8> { ... }
fn decode_parse_payload(buf: &[u8]) -> Result<DecodedParse, String> { ... }
```

OverrideContext under B-prime (per D-17 + RESEARCH §16.7 #6):

Final shape — retain `catalog_table_present: bool` + `is_file_backed: bool` as stateless config flags ONLY if they have readers; eliminate entirely otherwise. After Plan 02 revert restores the pre-Plan-02-partial shape (with `catalog: CatalogReader`), this task strips the `catalog: CatalogReader` field (the long-lived conn pointer) leaving only `catalog_table_present + is_file_backed`. Drop impl deletes (no resources to clean — just Box reclamation).

The two stateless flags are still consumed: `is_file_backed` gates DDL-time type inference (existing src/parse.rs usage), `catalog_table_present` short-circuits CatalogReader::lookup on read-only DBs whose `semantic_layer._definitions` was never created (Phase 63 RO-04). Whether sv_parse_function or sv_plan_function reads them at task time — if neither does, OverrideContext can retire entirely; if either does, retain the slim shape. Document the choice in 65-03-SUMMARY.md.

Init flow under B-prime (final shape of the relevant block within `init_extension` in `src/lib.rs`):
- DELETE: the `let mut catalog_conn ... duckdb_connect(db_handle, &mut catalog_conn)` block in `init_extension`. GONE.
- KEEP: the `catalog_table_present` probe block (search for `catalog_table_present` in `init_extension`) (it runs on the caller's `con` from duckdb-rs, NOT on a new long-lived conn — safe).
- DELETE: the `let catalog_reader = CatalogReader::new(catalog_conn, …);` line in `init_extension`. GONE.
- MODIFY: the `sv_register_parser_hooks(...)` call site in `init_extension` to read `sv_register_parser_hooks(db_handle, catalog_table_present, is_file_backed)` (NO catalog_conn argument; signature returns to Plan-02-partial form, which is now the intended target since the revert moved it back to the pre-Plan-02-partial form with a `duckdb_connection` arg).
  - This is a CHANGE from the post-revert signature in Task 1 of Plan 02. Update both cpp/src/shim.{cpp,hpp} declarations AND the Rust extern in src/lib.rs in lockstep.
  - The signature update here is what Plan 02 partial f9caafe tried to do — it stays this time because the parser_override deregistration + per-call probe shift removes the long-lived conn entirely (the unsuccessful attempt was the per-call probe from inside parser_override, NOT the signature change itself).
- KEEP (for Plans 04/05): the 14 `register_table_function_with_extra_info::<...>(name, &catalog_reader)?;` call sites for the read functions + 2 scalars (grep `register_table_function_with_extra_info` in `src/lib.rs`) and the `let mut query_conn ... duckdb_connect(...) ... QueryState` block (grep `let mut query_conn`). Plans 04/05/06 retire these.

Commits to revert from Plan 02-partial f9caafe are NOT re-introduced manually — instead the signature change naturally arises from this plan's deletions of catalog_conn. The signature shape "(duckdb_database, bool, bool)" coincides with what f9caafe tried but the route is different (this plan's deregistration of sv_parser_override removes the need for the conn in the first place).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Promote sv_parse_function — structural validation + payload stash (no catalog reads at parse time)</name>
  <files>
    src/parse.rs
    cpp/src/shim.cpp
    cpp/src/shim.hpp
  </files>
  <read_first>
    src/parse.rs
    cpp/src/shim.cpp
    cpp/src/shim.hpp
    .planning/phases/65-overridecontext-connection-teardown/65-RESEARCH.md
    .planning/phases/65-overridecontext-connection-teardown/65-PATTERNS.md
  </read_first>
  <behavior>
    - `sv_parse_function_rust` (re-purposed from today's `sv_parse_function_rust` extern declaration in `cpp/src/shim.cpp` — grep `extern "C" .*sv_parse_function_rust`) accepts the raw query string and the (possibly null) OverrideContext pointer. It detects the DDL prefix via `match_keyword_prefix` for each of the supported `DdlKind` verbs (Create / CreateOrReplace / CreateIfNotExists / Drop / DropIfExists / Describe / Show / ShowTerse / ShowColumns / Alter / AlterIfExists / ShowDimensions / ShowMetrics / ShowFacts / ShowMaterializations). For unrecognised prefixes returns `PARSE_NOT_OURS` (rc=0 in the existing encoding) — the default parser's error then surfaces.
    - For recognised prefixes: runs `validate_and_rewrite` (or the moral equivalent — the structural validator used by today's parser_override path; do NOT execute catalog reads here). On structural success, encodes `{verb, flags, byte_offset, view_name, validated_form}` via the new `encode_parse_payload` helper into a `Vec<u8>` and hands the pointer + length back to C++ via a new out-param. Returns the success rc (define new constant `PARSE_SUCCESS = 1` if not already present per existing rc encoding in the `sv_parse_stub` body — grep `PARSE_NOT_OURS|PARSE_SUCCESS` in `cpp/src/shim.cpp`).
    - For structural failure: returns the existing `DISPLAY_EXTENSION_ERROR` path with the carat-rendering `error_location` populated (Phase 62 caret tests gate). Behaviour byte-identical to today's sv_parse_function_rust failure path.
    - The C++ side stores the payload in `SemanticViewParseData::payload` (a new `vector<uint8_t>` field). `payload` survives until sv_plan_function reads it back.
    - sv_parse_function_rust MUST NOT touch any catalog. NO `ConnGuard::open`. NO `CatalogReader`. NO `duckdb_connect`. Purely structural / lexical / syntactic work.
  </behavior>
  <action>
    Extend the `SemanticViewParseData` class in `cpp/src/shim.cpp` (grep `class SemanticViewParseData`) to add `vector<uint8_t> payload`. Update its constructor and `Copy()` impl to clone the payload byte-vector (Copy is non-trivially called by DuckDB during plan rewriting; do not return nullptr — the existing `Copy()` returns a `make_uniq_base<ParserExtensionParseData, SemanticViewParseData>(query)`; extend to include the payload). Verify with the existing parser_override sqllogictest that Copy is still working after the change.

    In `src/parse.rs`, add:
    1. `encode_parse_payload(verb: DdlKind, flags: u8, byte_offset: u32, view_name: &str, validated_form: &str) -> Vec<u8>` — pure LE encoding per the format in RESEARCH §16.7 #3. Header: 1 byte verb + 1 byte flags + 4 byte byte_offset LE + 4 byte view_name_len LE + view_name bytes + 4 byte validated_form_len LE + validated_form bytes. Total = 14 bytes + view_name + validated_form. Add a unit test that round-trips an encode → decode for each `DdlKind`. Place in `#[cfg(test)] mod payload_tests` near the bottom of src/parse.rs (or a new src/parse/payload.rs sub-module if size warrants — author's call). Decode helper signature: `decode_parse_payload(buf: &[u8]) -> Result<DecodedParse, String>` where `DecodedParse { verb: DdlKind, flags: u8, byte_offset: u32, view_name: String, validated_form: String }`.
    2. Proptest: `proptest! { fn encode_decode_roundtrip(verb in any_ddlkind(), flags in any::<u8>(), byte_offset in any::<u32>(), view_name in "[a-zA-Z_][a-zA-Z0-9_]{0,32}", form in "[\\x20-\\x7e]{0,256}") { let buf = encode_parse_payload(verb, flags, byte_offset, &view_name, &form); let dec = decode_parse_payload(&buf).unwrap(); assert_eq!(dec.verb, verb); assert_eq!(dec.flags, flags); assert_eq!(dec.byte_offset, byte_offset); assert_eq!(dec.view_name, view_name); assert_eq!(dec.validated_form, form); } }`. Helper `any_ddlkind()` returns one of the 15 DdlKind variants uniformly.
    3. Modify `sv_parse_function_rust` (today declared in the `extern "C"` block at the top of `cpp/src/shim.cpp` — grep `sv_parse_function_rust` to find the declaration) to:
       a. Use `detect_ddl_kind` (existing helper or its analog — match_keyword_prefix loops) to find the DdlKind from the query prefix; return PARSE_NOT_OURS if none match.
       b. Run the existing structural validator (the same one parser_override used in v0.8.0 — search src/parse.rs for `validate_and_rewrite` or its current name; find the entry point that produces the validated form WITHOUT catalog reads).
       c. On structural failure: return DISPLAY_EXTENSION_ERROR with `error_location` (Phase 62 caret path — preserve byte-identical).
       d. On structural success: call `encode_parse_payload` and pass the resulting Vec<u8> back to C++ via two out params: `payload_ptr: *mut *mut u8`, `payload_len: *mut usize`. Use the existing `into_raw` pattern that the v0.8.0 parser_override code uses for error buffers (search cpp/src/shim.cpp + src/parse.rs for `sv_free_buffer` / `sv_drop_error_buffer` — there is already a Rust-allocated/C++-freed buffer protocol; reuse it).
       e. Update the C++ `sv_parse_function_rust` declaration in the `extern "C"` block at the top of `cpp/src/shim.cpp` to match the new signature with the payload out params.
       f. The existing parse_function dispatch (`sv_parse_stub` in `cpp/src/shim.cpp` — grep `sv_parse_stub`) is renamed to `sv_parse_function` and its body extended to read the payload out-params and store them on `SemanticViewParseData::payload` before returning.

    Build verification IN FOREGROUND:
    - `cargo build` exits 0; `cargo build --features extension --no-default-features` exits 0; `just build` exits 0.
    - `cargo test --lib parse::payload` passes (the round-trip + proptest).
    - `just test-sql > $TMPDIR/65_03_t1_sql.log 2>&1; tail -50 $TMPDIR/65_03_t1_sql.log` — depending on whether sv_parser_override is still wired at this point (it is, until Task 3), this should STILL show 47/47 PASS because Task 1 only adds the payload path WITHOUT removing the existing parser_override path. Both paths coexist mid-task.

    Commit boundary: `feat(65-03): sv_parse_function payload stash (structural-only, no catalog reads)`. Do NOT include the parser_override deregistration in this commit — Task 3 owns that.
  </action>
  <verify>
    <automated>cd /Users/paul/Documents/Dev/Personal/duckdb-semantic-views && grep -c "fn encode_parse_payload" src/parse.rs | grep -E "^[1-9]" && grep -c "fn decode_parse_payload" src/parse.rs | grep -E "^[1-9]" && grep -c "vector<uint8_t> payload" cpp/src/shim.cpp | grep -E "^[1-9]" && cargo test --lib parse > $TMPDIR/65_03_t1_test.log 2>&1 && grep -E "test result: ok" $TMPDIR/65_03_t1_test.log && just build > $TMPDIR/65_03_t1_build.log 2>&1 && just test-sql > $TMPDIR/65_03_t1_sql.log 2>&1 && tail -5 $TMPDIR/65_03_t1_sql.log | grep -E "47/47|47 tests"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "fn encode_parse_payload" src/parse.rs` ≥ 1; `grep -c "fn decode_parse_payload" src/parse.rs` ≥ 1.
    - Source: `grep -c "vector<uint8_t> payload" cpp/src/shim.cpp` ≥ 1 (new field on SemanticViewParseData).
    - Source: `grep -E "ConnGuard::open|CatalogReader::new|duckdb_connect" src/parse.rs | grep -E "sv_parse_function_rust|payload_tests"` returns 0 matches (parse path is catalog-free).
    - Test: `cargo test --lib parse::payload` passes.
    - Test: proptest `encode_decode_roundtrip` runs ≥256 cases (default proptest budget). Verify in test output.
    - Build: `just build` exits 0.
    - sqllogictest: `just test-sql` still 47/47 PASS (parser_override path coexists with the new payload path until Task 3).
    - Caret path preserved: `just test-caret` exits 0 (Phase 62 caret tests green — verify the DISPLAY_EXTENSION_ERROR path with `error_location` still fires).
  </acceptance_criteria>
  <done>
    Structural parse + payload stash is in place. sv_parse_function returns PARSE_SUCCESS with a serialised payload for recognised DDL prefixes; parser_override path is still wired (Task 3 retires it). All catalog work has been pulled out of parse-time. sqllogictest 47/47 PASS preserved.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement sv_emit_native_sql_rust + sv_plan_function — per-call probe + native SQL emission</name>
  <files>
    src/parse.rs
    cpp/src/shim.cpp
    cpp/src/shim.hpp
  </files>
  <read_first>
    src/parse.rs
    cpp/src/shim.cpp
    .planning/phases/65-overridecontext-connection-teardown/65-RESEARCH.md
    .planning/phases/65-overridecontext-connection-teardown/65-OPTION-B-SPIKE.md
    .planning/phases/65-overridecontext-connection-teardown/65-PATTERNS.md
    src/conn_guard.rs
    src/catalog.rs
  </read_first>
  <behavior>
    - `sv_emit_native_sql_rust(conn: duckdb_connection, payload_ptr: *const u8, payload_len: usize, native_sql_out: *mut *mut c_char, native_sql_len_out: *mut usize, error_msg_out: *mut *mut c_char) -> bool`: decodes the payload via `decode_parse_payload`; constructs a `CatalogReader::new(conn, catalog_table_present)` (catalog_table_present can be re-probed from the per-call conn cheaply or carried via a global from init — for v1 re-probe per-call); runs the existing catalog-read + native-SQL emission helpers (the same ones the v0.8.0 parser_override path used — `rewrite_create` / `rewrite_drop_or_alter` / `emit_native_create_sql`); writes the rewritten SQL into a Rust-allocated CString returned via `native_sql_out`. On any error returns false with an error_msg_out.
    - `sv_plan_function(ClientContext &context, ParserExtensionInfo *info, unique_ptr<ParserExtensionParseData> parse_data_ptr) -> unique_ptr<TableRef>`: extracts payload from `SemanticViewParseData`; opens `Connection probe(*context.db)` in a try/catch block; reinterpret_casts to `duckdb_connection`; calls `sv_emit_native_sql_rust` with the probe conn; drops probe before further work (RAII scope); converts the returned native_sql string into a TableRef via the existing Phase 58 parser_override path's conversion routine (a TableRef whose binder evaluation produces the INSERT/DELETE RETURNING on the caller's conn). On Rust-side failure, throws ParserException.
    - The catalog WRITE (the rewritten INSERT/DELETE) does NOT run on `probe`. It is returned as SQL string and emitted via ParserExtensionPlanResult / TableRef for the binder to execute on the caller's connection — preserving transactional DDL (D-20).
    - Per-call probe MUST NOT outlive sv_plan_function's stack frame. RAII guarantees this via Connection's destructor.
  </behavior>
  <action>
    1) In src/parse.rs, add `#[unsafe(no_mangle)] pub unsafe extern "C" fn sv_emit_native_sql_rust(...)`. Body:
       - `let payload = std::slice::from_raw_parts(payload_ptr, payload_len);`
       - Wrap the body in `catch_unwind(AssertUnwindSafe(|| { ... }))` (mandatory at every Rust↔C boundary, per PATTERNS.md FFI signature pattern).
       - `let decoded = decode_parse_payload(payload).map_err(|e| ...)?;`
       - `let catalog_table_present = probe_catalog_present(conn);` (helper: run a cheap `SELECT 1 FROM information_schema.tables WHERE table_schema='semantic_layer' AND table_name='_definitions' LIMIT 1` against `conn`; return bool; reuse if there is an existing helper in src/catalog.rs).
       - `let catalog = CatalogReader::new(conn, catalog_table_present);`
       - `match decoded.verb { DdlKind::Create | DdlKind::CreateOrReplace | DdlKind::CreateIfNotExists => emit_native_create_sql(&catalog, &decoded, /*is_file_backed=*/ ?, &mut native_sql)?, DdlKind::Drop | DdlKind::DropIfExists | DdlKind::Alter | DdlKind::AlterIfExists => emit_native_drop_or_alter_sql(&catalog, &decoded, &mut native_sql)?, /* DESCRIBE / SHOW variants — emit the same rewritten SQL the v0.8.0 path emits */ _ => emit_native_describe_or_show_sql(&catalog, &decoded, &mut native_sql)? }`
       - Resolve `is_file_backed` — it was on OverrideContext before. Two options: (a) re-probe via `current_setting('access_mode')` against the per-call conn (one extra SELECT per DDL — cheap), or (b) include `is_file_backed` in the payload at parse time (sv_parse_function has access to OverrideContext::is_file_backed). Choose (b) — extend `encode_parse_payload` from Task 1 to include `is_file_backed: bool` (1 byte) in the flags field, document the flag mask in the encoder comment.
       - Write the resulting `native_sql` into a `CString::new(native_sql).unwrap().into_raw()` and set `*native_sql_out = raw; *native_sql_len_out = sql_len;`.
       - Return `true` on success.

       NB: the helpers `emit_native_create_sql`, `emit_native_drop_or_alter_sql`, `emit_native_describe_or_show_sql` already exist as part of the v0.8.0 parser_override flow (they may have slightly different names — search src/parse.rs for `rewrite_create`, `rewrite_drop_or_alter`, `rewrite_to_native_sql`, etc.). Reuse the existing helpers verbatim; only the connection source changes (per-call probe instead of long-lived catalog_conn).

    2) In cpp/src/shim.cpp, rename `sv_plan_unreachable` to `sv_plan_function` (or add a new function and update the registration in `sv_register_parser_hooks` to point `ext.plan_function = sv_plan_function;`). Implement body per RESEARCH §16.2 step 3 / the canonical shape in CONTEXT.md `<specifics>`. Construct `Connection probe(*context.db);` inside try/catch; reinterpret_cast to `duckdb_connection`; call `sv_emit_native_sql_rust`. Use the existing Phase 58 helper that converts a SQL string into a `unique_ptr<TableRef>` for the parser-extension plan path — search cpp/src/shim.cpp + the surrounding Phase 58 code in v0.8.0 history for the converter; the v0.8.0 parser_override path emitted native SQL and built a TableRef from it, that conversion code is what this task reuses verbatim.

    3) Add the new Rust extern in cpp/src/shim.cpp's extern "C" block:
       ```cpp
       bool sv_emit_native_sql_rust(
           duckdb_connection conn,
           const uint8_t *payload, size_t payload_len,
           char **native_sql_out, size_t *native_sql_len_out,
           char **error_msg_out);
       ```
       and a matching Rust declaration in src/parse.rs (verify with `grep`).

    4) Build verification IN FOREGROUND:
       - `cargo build` exits 0; `cargo build --features extension --no-default-features` exits 0; `just build` exits 0.
       - `just test-sql > $TMPDIR/65_03_t2_sql.log 2>&1; tail -50 $TMPDIR/65_03_t2_sql.log` — STILL 47/47 PASS (sv_plan_function exists but sv_parser_override is still wired and short-circuits before sv_plan_function for the current DDL inputs — both paths coexist until Task 3).
       - Add ONE new sqllogictest at `test/sql/65_plan_function_smoke.test` that explicitly disables parser_override via a SET (if such a knob exists — otherwise gate at the sv_register_parser_hooks level temporarily for this test). The test runs a `CREATE SEMANTIC VIEW v AS TABLES (t AS some_table PRIMARY KEY (id)) DIMENSIONS (t.x AS t.x) METRICS (t.c AS COUNT(*))` and asserts it succeeds via the plan_function path. The test is OPTIONAL evidence — if no easy parser_override-disable knob exists, defer the smoke to Task 3 where the deregistration is the production change.

    Commit: `feat(65-03): sv_plan_function with per-call Connection(*context.db) + native SQL emission`.
  </action>
  <verify>
    <automated>cd /Users/paul/Documents/Dev/Personal/duckdb-semantic-views && grep -c "fn sv_emit_native_sql_rust" src/parse.rs | grep -E "^[1-9]" && grep -c "sv_plan_function" cpp/src/shim.cpp | grep -E "^[1-9]" && grep -E "Connection probe\(\*context.db\)" cpp/src/shim.cpp && just build > $TMPDIR/65_03_t2_build.log 2>&1 && just test-sql > $TMPDIR/65_03_t2_sql.log 2>&1 && tail -5 $TMPDIR/65_03_t2_sql.log | grep -E "47/47|47 tests"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "pub unsafe extern \"C\" fn sv_emit_native_sql_rust" src/parse.rs` == 1.
    - Source: `grep -c "ext.plan_function = sv_plan_function" cpp/src/shim.cpp` == 1 (registered as the plan-function entry).
    - Source: `grep -E "Connection probe\(\*context.db\)" cpp/src/shim.cpp` returns ≥1 match (the canonical B-prime per-call ctor).
    - Source: `grep -c "reinterpret_cast<duckdb_connection>(&probe)" cpp/src/shim.cpp` ≥ 1.
    - Source: sv_emit_native_sql_rust body uses CatalogReader::new with the per-call conn argument: `grep -B 2 "CatalogReader::new" src/parse.rs | grep -c "conn"` ≥ 1.
    - No long-lived caching in the emission helper: `grep -E "static.*duckdb_connection|OnceLock.*duckdb_connection" src/parse.rs` returns 0 matches.
    - Build green: `cargo build`, `cargo build --features extension --no-default-features`, `just build` all exit 0.
    - sqllogictest still green: `just test-sql` reports 47/47 PASS (parser_override coexists with plan_function until Task 3).
    - Transactional DDL semantics preserved (no behaviour regression yet — Task 3 is the cutover): `just test-adbc` (if it runs in-tree) or the existing transactional DDL sqllogictest is part of the 47-test PASS count.
  </acceptance_criteria>
  <done>
    sv_plan_function exists and uses the per-call C++ Connection probe to do catalog reads via the existing Rust emission helpers. parser_override still owns the production write path on disk; Task 3 cuts over.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Deregister sv_parser_override + retire H1 catalog_conn + OverrideContext slim shape</name>
  <files>
    cpp/src/shim.cpp
    cpp/src/shim.hpp
    src/lib.rs
    src/parse.rs
  </files>
  <read_first>
    cpp/src/shim.cpp
    cpp/src/shim.hpp
    src/lib.rs
    src/parse.rs
    .planning/phases/65-overridecontext-connection-teardown/65-CONTEXT.md
    .planning/phases/65-overridecontext-connection-teardown/65-RESEARCH.md
  </read_first>
  <behavior>
    - `sv_register_parser_hooks` no longer assigns `ext.parser_override`. The C++ `sv_parser_override` function and its Rust counterpart `sv_parser_override_rust` are either deleted or marked `#[deprecated]` + `__attribute__((unused))` with an explanatory comment (D-22 path (a): full deregistration is cleaner; do that). The match_keyword_prefix logic that lived in `sv_parser_override_rust` moves entirely into `sv_parse_function_rust` (already done in Task 1).
    - `sv_register_parser_hooks` signature is now `(duckdb_database db_handle, bool catalog_table_present, bool is_file_backed)` — NO duckdb_connection parameter. The C++ helper builds (or skips) an `OverrideContext` from the two flags only.
    - OverrideContext shape under B-prime: keep `catalog_table_present: bool + is_file_backed: bool` if either is still consumed by sv_parse_function_rust (Task 1's payload encoder needs is_file_backed at parse time — keep). Remove `catalog: CatalogReader`. Remove the custom `Drop` impl with the INTENTIONAL LEAK comment (no resources to clean — Box reclamation is automatic). The PHASE-65-GUARD marker comment is RESTORED in the struct body (it was removed by the Plan 02 partial revert in Plan 02 Task 1; bring it back here with updated content: `// PHASE-65-GUARD (B-prime): never reintroduce duckdb_connection or db_handle. Catalog reads run on per-call probe inside sv_plan_function only.`).
    - In `init_extension` (src/lib.rs): the H1 `catalog_conn` open block (`let mut catalog_conn ... duckdb_connect(db_handle, &mut catalog_conn)`) is DELETED. The `let catalog_reader = CatalogReader::new(catalog_conn, …)` line is DELETED. The `sv_register_parser_hooks(...)` call site is updated to pass only `(db_handle, catalog_table_present, is_file_backed)`. The 14 `register_table_function_with_extra_info` call sites still pass `&catalog_reader` — but `catalog_reader` is gone. **Mid-task transition**: synthesise a `catalog_reader_legacy` value that wraps a freshly-opened `ConnGuard`'s raw connection so the existing `register_table_function_with_extra_info` calls continue to receive a CatalogReader-shaped extra_info — OR migrate those 14 calls to a temporary `CatalogReader::new(null_conn, catalog_table_present)` placeholder that Plans 04/05 then replace.
       - Decision: synthesise a TEMPORARY long-lived `catalog_conn_temp` here (opened with `duckdb_connect(db_handle)`) and use it for the 14 read-side `register_table_function_with_extra_info` calls. This is a STEP BACKWARD relative to Plan 06's final state (it's still a long-lived conn), but it lets us land Plan 03 without simultaneously rewriting the read path. Plans 04 + 05 + 06 retire `catalog_conn_temp`. Document the temporary nature in a TEMP-PLAN-04 comment block in src/lib.rs.
       - Rationale: this preserves Plans 04/05 as parallelisable AND keeps Plan 03 atomically committable / rollback-safe. The alternative (delete `catalog_conn_temp` here AND rewrite the read path inline) violates the plan's single-concern principle.

       NB: this temporary still leaks Connection lifetime — so the B1..B11 watchdog tests stay RED at end of Plan 03. They flip GREEN at end of Plan 06.

    - sv_register_parser_hooks's C++ body: stop assigning `ext.parser_override`; keep `ext.parse_function = sv_parse_function` and `ext.plan_function = sv_plan_function`; keep the SemanticViewsParserInfo allocation + sv_make_override_context(db_handle, catalog_table_present, is_file_backed). sv_make_override_context's Rust signature now matches `(duckdb_database, bool, bool) -> *mut c_void`.
    - The Phase 62 caret-rendering surface (DISPLAY_EXTENSION_ERROR + error_location) is preserved — it lives in sv_parse_function_rust's failure path, not in the removed parser_override.
    - INTENTIONAL LEAK comment block in src/parse.rs is REMOVED (Plan 02 Task 1's revert restored it; this task removes it permanently because there is no long-lived conn on OverrideContext anymore — D-22 finding).
  </behavior>
  <action>
    1) Delete `sv_parser_override` from `cpp/src/shim.cpp` (the entire `static ParserOverrideResult sv_parser_override(...)` definition — grep `static ParserOverrideResult sv_parser_override`) and remove the `extern "C" uint8_t sv_parser_override_rust(...)` declaration in the `extern "C"` block at the top of `cpp/src/shim.cpp` (grep `sv_parser_override_rust`). Remove the corresponding Rust definition `sv_parser_override_rust` from src/parse.rs (search for `pub unsafe extern "C" fn sv_parser_override_rust`; delete the entire function + its catch_unwind body).

    2) Modify `sv_register_parser_hooks` in cpp/src/shim.cpp:
       - Body: remove the line `ext.parser_override = sv_parser_override;`.
       - Keep `ext.parse_function = sv_parse_function;`.
       - Add (or update if present) `ext.plan_function = sv_plan_function;`.
       - Signature now `(duckdb_database db_handle, bool catalog_table_present, bool is_file_backed)` — drop the duckdb_connection argument.
       - Update the call to `sv_make_override_context` accordingly: `sv_make_override_context(db_handle, catalog_table_present, is_file_backed)` (no conn).
       - Update cpp/src/shim.hpp's declaration to match.

    3) Modify `sv_make_override_context` in src/parse.rs:
       - New signature: `pub unsafe extern "C" fn sv_make_override_context(db: ffi::duckdb_database, catalog_table_present: bool, is_file_backed: bool) -> *mut c_void`.
       - Body constructs `Box<OverrideContext { catalog_table_present, is_file_backed }>` and returns Box::into_raw.
       - NB: the `db` argument is currently unused in the slim shape — keep it in the signature for future-proofing (sv_parse_function_rust might want db_handle for some niche path; mark `_db: ffi::duckdb_database` if unused now).

    4) Modify `OverrideContext` struct in src/parse.rs:
       - Final shape: `pub struct OverrideContext { pub catalog_table_present: bool, pub is_file_backed: bool }`.
       - NO `db_handle` field. NO `catalog: CatalogReader` field.
       - Remove the custom `Drop` impl (the INTENTIONAL LEAK Phase 62 Q2 block) — there are no resources to clean.
       - Add the PHASE-65-GUARD comment as the first line inside the struct body: `// PHASE-65-GUARD (B-prime): never reintroduce duckdb_connection or db_handle here. Catalog reads run on a per-call C++ Connection(*context.db) probe inside sv_plan_function only — see RESEARCH §16.2.`
       - Keep `unsafe impl Send + Sync for OverrideContext` (the two bools are trivially threadsafe; impls are cheap insurance against future refactors).

    5) Modify `init_extension` in `src/lib.rs`:
       - DELETE the catalog_conn open + comment block (the `let mut catalog_conn ... duckdb_connect(db_handle, &mut catalog_conn)` lines plus the comment block immediately above them).
       - DELETE the `let catalog_reader = CatalogReader::new(catalog_conn, …);` line in `init_extension`.
       - REPLACE with a temporary `catalog_conn_temp` open + `catalog_reader = CatalogReader::new(catalog_conn_temp, …)` for the 14 read-side function registrations. Mark with a `// TEMP-PLAN-04: temporary long-lived conn retained for read-side registrations.\n// Retired by Plans 04+05 (port to sv_register_table_function) and Plan 06 (delete this open entirely).` comment block above the open. Apply the same comment block above the `let mut query_conn ... duckdb_connect(...) ... QueryState` block (grep `let mut query_conn` in `init_extension`).
       - UPDATE the `sv_register_parser_hooks(...)` call site in `init_extension` to: `sv_register_parser_hooks(db_handle, catalog_table_present, is_file_backed)` — three args, NO catalog_conn.
       - UPDATE the Rust `extern "C" { fn sv_register_parser_hooks(...) }` declaration in src/lib.rs to match the new C++ signature.

    6) Run a smoke verification:
       - `cargo build` exits 0; `cargo build --features extension --no-default-features` exits 0; `just build` exits 0.
       - `just test-sql > $TMPDIR/65_03_t3_sql.log 2>&1; tail -50 $TMPDIR/65_03_t3_sql.log` — MUST still be 47/47 PASS. This is the critical gate: the sv_plan_function path is now the success path; if any rewrite_create / rewrite_drop / DESCRIBE / SHOW path was missing in the sv_emit_native_sql_rust dispatch from Task 2, sqllogictest will fail here. Diagnose by adding `fprintf(stderr, "[B-PRIME] sv_plan_function entered for verb=...")` traces and re-running the broken test in isolation — common cause: a DDL verb's emission helper was not wired into the dispatch in Task 2.
       - `just test-caret > $TMPDIR/65_03_t3_caret.log 2>&1; tail -20 $TMPDIR/65_03_t3_caret.log` — MUST be green (Phase 62 caret tests preserved via sv_parse_function's DISPLAY_EXTENSION_ERROR path).
       - `just test-adbc > $TMPDIR/65_03_t3_adbc.log 2>&1; tail -20 $TMPDIR/65_03_t3_adbc.log` — MUST be green (Phase 58 ADBC transactional DDL preserved — the catalog write rides the binder onto the caller's conn, NOT the per-call probe).

    Commit: `feat(65-03): deregister sv_parser_override; OverrideContext slim shape; H1 catalog_conn retired; plan_function is the write-path entry`.
  </action>
  <verify>
    <automated>cd /Users/paul/Documents/Dev/Personal/duckdb-semantic-views && [ "$(grep -c 'ext.parser_override' cpp/src/shim.cpp)" -eq 0 ] && grep -c "ext.plan_function = sv_plan_function" cpp/src/shim.cpp | grep -E "^[1-9]" && [ "$(grep -c 'sv_parser_override_rust' src/parse.rs)" -eq 0 ] && [ "$(grep -c 'INTENTIONAL LEAK' src/parse.rs)" -eq 0 ] && grep -E "PHASE-65-GUARD.*B-prime" src/parse.rs && grep -E "sv_register_parser_hooks.*db_handle.*catalog_table_present.*is_file_backed" cpp/src/shim.hpp && just build > $TMPDIR/65_03_t3_build.log 2>&1 && just test-sql > $TMPDIR/65_03_t3_sql.log 2>&1 && tail -5 $TMPDIR/65_03_t3_sql.log | grep -E "47/47|47 tests"</automated>
  </verify>
  <acceptance_criteria>
    - Deregistration: `grep -c "ext.parser_override" cpp/src/shim.cpp` == 0 (no assignment of parser_override anywhere).
    - Deregistration: `grep -c "sv_parser_override\b" cpp/src/shim.cpp` == 0 (function definition deleted).
    - Deregistration: `grep -c "sv_parser_override_rust" src/parse.rs` == 0 (Rust counterpart deleted).
    - sv_plan_function wired: `grep -c "ext.plan_function = sv_plan_function" cpp/src/shim.cpp` == 1.
    - sv_register_parser_hooks signature: `grep -E "sv_register_parser_hooks\(.*duckdb_database.*bool.*bool" cpp/src/shim.hpp` returns ≥1 match (NO duckdb_connection param).
    - OverrideContext final shape: `grep -E "pub struct OverrideContext" -A 5 src/parse.rs | grep -E "catalog_table_present|is_file_backed"` returns ≥2 matches; the same grep slice MUST NOT contain `duckdb_connection`, `duckdb_database`, `db_handle`, or `CatalogReader`.
    - PHASE-65-GUARD marker restored: `grep -c "PHASE-65-GUARD" src/parse.rs` == 1.
    - INTENTIONAL LEAK comment removed: `grep -c "INTENTIONAL LEAK" src/parse.rs` == 0.
    - H1 retired: `grep -E "let mut catalog_conn:" src/lib.rs | grep -v "^[[:space:]]*//"` returns 0 matches (the long-lived catalog_conn is gone — only the TEMP-PLAN-04 marker survives until Plan 06).
    - TEMP-PLAN-04 marker present: `grep -c "TEMP-PLAN-04" src/lib.rs` ≥ 1 (acknowledges the temporary read-side long-lived conn that Plans 04+05+06 retire).
    - Build green: `cargo build`, `cargo build --features extension --no-default-features`, `just build` all exit 0.
    - sqllogictest green: `just test-sql` reports 47/47 PASS.
    - Caret tests green: `just test-caret` exits 0 (Phase 62 surface preserved).
    - ADBC transactional DDL green: `just test-adbc` exits 0 (D-20 non-negotiable).
    - LIFE-01 partial progress (acceptable to remain red until Plan 06): the B1/B2/B3/B4/B11 watchdog tests in test/integration/test_readonly_load.py STILL fail at end of Plan 03 because the read path's H2/H1-temp long-lived conn is still open. This is acceptable mid-phase — not a Plan 03 gate. The LIFE-01 final gate is Plan 06 verification.
  </acceptance_criteria>
  <done>
    parser_override is gone from the codebase. The write path runs through sv_parse_function (structural) + sv_plan_function (per-call probe + emission via the caller's binder). v0.8.0 transactional DDL is byte-identical preserved. Phase 62 caret rendering is byte-identical preserved. H1 catalog_conn is retired (only a TEMP-PLAN-04 conn temporarily survives for the 14 read-side functions, slated for removal in Plans 04/05/06). LIFE-02 mechanism (per-call C++ probe at every callback that has ClientContext &) is in place for the write path.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller's parse thread ↔ sv_parse_function | No catalog access; pure structural validation |
| Caller's plan thread ↔ sv_plan_function | Per-call Connection probe inside C++; reinterpret_cast to duckdb_connection passed to Rust; probe destructs before sv_plan_function returns |
| sv_plan_function ↔ caller's conn | Native SQL emitted via ParserExtensionPlanResult → TableRef; executed by binder on caller's conn — transactional DDL contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-03-01 | Tampering | sv_emit_native_sql_rust receives a Rust-allocated CString; C++ must free it via sv_free_cstring | mitigate | Task 2 acceptance check that sv_free_cstring is called after `string(native_sql, len)` materialisation. |
| T-65-03-02 | Denial of Service | Connection probe throws inside sv_plan_function — caller sees ParserException | accept | Same surface as Phase 58 parser_override path's error reporting. Documented behaviour. |
| T-65-03-03 | Tampering | Catalog WRITE accidentally routed onto per-call probe instead of caller's conn — would break transactional DDL (D-20) | mitigate | Task 2 explicitly only does READS on probe; the emitted INSERT/DELETE SQL is returned as a string and run by the binder on the caller's conn. Task 3 acceptance criterion `just test-adbc` exits 0 verifies behaviour. Read-after-write inside the same statement uses probe (which sees only committed state — TECH-DEBT 19 carries over unchanged). |
| T-65-03-04 | Information Disclosure | Caret-rendering DISPLAY_EXTENSION_ERROR path drops on parser_override removal | mitigate | Task 1 keeps the failure path in sv_parse_function_rust; Task 3 acceptance criterion `just test-caret` exits 0. |
| T-65-03-SC | Tampering | No new crate dependencies | accept | Plan 03 is pure refactor within existing FFI surface. |
</threat_model>

<verification>
After all three tasks complete:

1. `just test-sql` reports 47/47 PASS.
2. `just test-caret` exits 0 (Phase 62 caret rendering preserved).
3. `just test-adbc` exits 0 (Phase 58 ADBC transactional DDL preserved — D-20 non-negotiable).
4. `just test-all` exits 0 (full suite — though B1..B4 + B11 in test_readonly_load.py still red because read path's TEMP-PLAN-04 long-lived conn is still open; this is acceptable mid-phase, see acceptance criteria for the explanation. Per CLAUDE.md the phase quality gate is end-of-phase `just test-all`, not Plan 03 gate).
5. `grep -c "ext.parser_override" cpp/src/shim.cpp` == 0.
6. `grep -c "sv_parser_override_rust" src/parse.rs` == 0.
7. `grep -c "fn sv_emit_native_sql_rust" src/parse.rs` ≥ 1.
8. `grep -c "Connection probe" cpp/src/shim.cpp` ≥ 1.
9. OverrideContext shape verified by reading the struct definition (catalog_table_present + is_file_backed only, no conn field, no db_handle field).
10. Symbol audit: `nm build/debug/semantic_views.duckdb_extension | grep parser_override` returns 0 matches (parser_override is gone from the binary).
</verification>

<success_criteria>
- sv_parser_override deregistered + deleted (file shrinks, no dead-code marker survives).
- sv_parse_function + sv_plan_function are the production write-path entries.
- Per-call C++ Connection probe inside sv_plan_function is the catalog-read mechanism (B-prime).
- v0.8.0 transactional DDL semantics preserved byte-identical (D-20 non-negotiable).
- Phase 62 caret rendering preserved byte-identical.
- H1 catalog_conn retired from init_extension; only TEMP-PLAN-04 conn survives temporarily for the 14 read-side functions (Plans 04/05/06 retire).
- LIFE-02 mechanism documented in code via PHASE-65-GUARD marker comment.
- LIFE-01 watchdog tests will still be red at end of Plan 03 (read path not yet ported) — acceptable mid-phase; Plan 06 is the final gate.
</success_criteria>

<output>
Create `.planning/phases/65-overridecontext-connection-teardown/65-03-SUMMARY.md` when done. The summary MUST:
- State the OverrideContext final shape (which fields survived, which retired; whether the struct could be retired entirely or required at least one consumer).
- State the disposition of the `is_file_backed` flag — whether it travels via OverrideContext (sv_parse_function reads it) or is re-probed at plan time, with rationale.
- Confirm 47/47 sqllogictest PASS + caret + ADBC green.
- Document the TEMP-PLAN-04 marker placement in src/lib.rs (the temporary catalog_conn_temp + query_conn that Plans 04/05/06 retire).
- Confirm the B1..B4 + B11 watchdog tests are still red AND why this is expected (read path not yet ported).
- Note any deviation from RESEARCH §16.2 / §16.4 and why.
</output>
