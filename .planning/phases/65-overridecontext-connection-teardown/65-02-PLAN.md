---
phase: 65-overridecontext-connection-teardown
plan: 02
type: execute
wave: 2
depends_on:
  - 65-01
files_modified:
  - src/parse.rs
  - src/lib.rs
  - cpp/src/shim.cpp
  - cpp/src/shim.hpp
autonomous: true
requirements:
  - LIFE-01
  - LIFE-02
tags:
  - duckdb
  - rust
  - ffi
  - parser_override
  - lifecycle

must_haves:
  truths:
    - "`OverrideContext` stores `db_handle: duckdb_database` and `is_file_backed: bool`; it does NOT carry a `duckdb_connection` or a `CatalogReader`"
    - "`Drop for OverrideContext` is removed (or reduced to a no-op) — there is no longer any `INTENTIONAL LEAK` comment to maintain"
    - "`sv_register_parser_hooks` C++ signature no longer takes `duckdb_connection`; only `duckdb_database` + `is_file_backed`"
    - "`sv_make_override_context` Rust FFI entrypoint takes `(db: duckdb_database, is_file_backed: bool)` and panic-guards with `catch_unwind(AssertUnwindSafe(...))`"
    - "Every catalog read inside `rewrite_to_native_sql`, `rewrite_drop_or_alter`, `rewrite_create`, and the enrichment path opens a fresh `ConnGuard` from `ctx.db_handle` and drops it before return"
    - "`src/lib.rs::init_extension` no longer calls `duckdb_connect` to create the H1 `catalog_conn` (the per-call ConnGuard makes that connection ephemeral); `catalog_conn` removal happens here, `query_conn` (H2) removal is Plan 03's job"
    - "All existing Phase 62 transactional DDL sqllogictests (`test/sql/v080_transactional_ddl.test`) and caret tests still pass byte-identical"
  artifacts:
    - path: "src/parse.rs"
      provides: "OverrideContext field swap + per-call ConnGuard in rewrite_* paths"
      contains: "db_handle: libduckdb_sys::duckdb_database"
    - path: "cpp/src/shim.cpp"
      provides: "sv_register_parser_hooks updated signature"
      contains: "sv_register_parser_hooks"
    - path: "src/lib.rs"
      provides: "init_extension no longer owns catalog_conn"
      contains: "sv_register_parser_hooks"
  key_links:
    - from: "src/parse.rs (rewrite_create / rewrite_drop_or_alter / enrichment)"
      to: "src/conn_guard.rs::ConnGuard"
      via: "ConnGuard::open(ctx.db_handle) per call"
      pattern: "ConnGuard::open"
    - from: "src/lib.rs::init_extension"
      to: "cpp/src/shim.cpp::sv_register_parser_hooks"
      via: "FFI call with db_handle + is_file_backed (no conn)"
      pattern: "sv_register_parser_hooks"
---

<objective>
Refactor the parser_override path to eliminate the long-lived `catalog_conn` (H1 in RESEARCH §5). `OverrideContext` carries
`db_handle: duckdb_database` instead of a `CatalogReader`; every catalog read inside `parser_override` opens a fresh
`ConnGuard` and drops it before returning. The C++ shim's `sv_register_parser_hooks` signature loses its `duckdb_connection`
parameter; `sv_make_override_context` likewise.

Purpose: H1 is one of the two `shared_ptr<DatabaseInstance>` references that keep the user's DB alive past `close()`. Removing
it is half of the LIFE-01 / LIFE-02 fix. This plan also closes Phase 62 §Q2's "intentional bounded leak" by making the
question moot — there is no connection to leak at parser-info scope.

Output: parser_override path operates on per-call connections; H1 is gone; Phase 62 transactional DDL tests stay green.
</objective>

<execution_context>
@/Users/paul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-overridecontext-connection-teardown/65-CONTEXT.md
@.planning/phases/65-overridecontext-connection-teardown/65-RESEARCH.md
@.planning/phases/65-overridecontext-connection-teardown/65-PATTERNS.md
@.planning/phases/65-overridecontext-connection-teardown/65-VALIDATION.md
@.planning/phases/65-overridecontext-connection-teardown/65-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Current OverrideContext shape (being replaced) — src/parse.rs:46-71 -->
```rust
#[cfg(feature = "extension")]
pub struct OverrideContext {
    pub catalog: crate::catalog::CatalogReader,  // carries leaked duckdb_connection
    pub is_file_backed: bool,
}
#[cfg(feature = "extension")]
impl Drop for OverrideContext { /* Phase 62 §Q2 intentional leak */ }
```

<!-- Target OverrideContext shape -->
```rust
#[cfg(feature = "extension")]
pub struct OverrideContext {
    pub db_handle: libduckdb_sys::duckdb_database,
    pub is_file_backed: bool,
}
// No custom Drop — no owned resources beyond the Box itself.
```

<!-- Current sv_make_override_context (src/parse.rs:2461-2484) -->
```rust
#[no_mangle]
pub unsafe extern "C" fn sv_make_override_context(
    catalog_conn: libduckdb_sys::duckdb_connection,
    is_file_backed: bool,
) -> *mut std::ffi::c_void { ... }
```

<!-- Target sv_make_override_context -->
```rust
#[no_mangle]
pub unsafe extern "C" fn sv_make_override_context(
    db: libduckdb_sys::duckdb_database,
    is_file_backed: bool,
) -> *mut std::ffi::c_void { ... }
```

<!-- ConnGuard usage pattern at every catalog-read site -->
```rust
let guard = unsafe { crate::conn_guard::ConnGuard::open(ctx.db_handle) }
    .map_err(|e| /* convert to phase's ParseError shape */)?;
let catalog = crate::catalog::CatalogReader::new(guard.raw(), /*catalog_table_present=*/ true);
let exists = catalog.exists(&name).map_err(...)?;
// `guard` drops at end of scope → duckdb_disconnect
```

<!-- Existing analog: execute_sql_raw call sites in src/parse.rs:1989-2035 -->
```rust
unsafe { crate::query::table_function::execute_sql_raw(ctx.catalog.raw(), &read_sql) }
// becomes:
unsafe { crate::query::table_function::execute_sql_raw(guard.raw(), &read_sql) }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Swap `OverrideContext` to `db_handle`; update Rust FFI entrypoint `sv_make_override_context`</name>
  <files>
    src/parse.rs
  </files>
  <read_first>
    src/parse.rs
    src/conn_guard.rs
    src/catalog.rs
    .planning/phases/65-overridecontext-connection-teardown/65-PATTERNS.md
    .planning/phases/65-overridecontext-connection-teardown/65-RESEARCH.md
  </read_first>
  <behavior>
    - `OverrideContext` no longer compiles with a `catalog: CatalogReader` field. The new field is `db_handle: libduckdb_sys::duckdb_database`.
    - `Drop for OverrideContext` is removed entirely (no resources to free beyond the Box).
    - `sv_make_override_context` accepts `(db: duckdb_database, is_file_backed: bool)` and constructs the new shape; panic-guarded.
    - `sv_drop_override_context` (existing) continues to work — it only frees the Box, no connection logic.
    - Every `rewrite_*` site and enrichment call inside `parse.rs` that previously read `ctx.catalog.exists`, `ctx.catalog.lookup`,
      `ctx.catalog.raw()`, or any other `CatalogReader` method now opens a per-call `ConnGuard`, constructs a transient
      `CatalogReader::new(guard.raw(), true)`, performs its work, and the guard drops at scope end.
    - All Phase 62 transactional DDL sqllogictests pass byte-identical post-refactor.
  </behavior>
  <action>
    Edit `src/parse.rs` to perform the field swap and rewire every catalog read.

    Step 1 — Replace the `OverrideContext` definition (currently at `src/parse.rs:46-71`):
    - Remove the `catalog: crate::catalog::CatalogReader` field; add `pub db_handle: libduckdb_sys::duckdb_database`.
    - Keep `pub is_file_backed: bool`.
    - Remove the entire `impl Drop for OverrideContext` block (including the Phase 62 §Q2 leak comment). Replace it with a
      single short comment: `// No custom Drop — Box reclaims itself; the db_handle is a non-owning pointer (see Phase 65 RESEARCH §3.3).`
    - Adjust any `Send`/`Sync` impl bounds: the new struct's only non-trivially-Send field is a raw pointer; mirror whatever
      `Send`/`Sync` declaration the current `OverrideContext` carries (currently none — relies on the underlying types). If
      `register_table_function_with_extra_info` or `parser_extension` Send/Sync requirements demand it, add
      `unsafe impl Send for OverrideContext {}` with a SAFETY comment cribbed from `src/catalog.rs:108-112`.

    Step 2 — Update `sv_make_override_context` (currently at `src/parse.rs:2461-2484`):
    - Change the `catalog_conn: libduckdb_sys::duckdb_connection` parameter to `db: libduckdb_sys::duckdb_database`.
    - Inside the `catch_unwind(AssertUnwindSafe(...))` body, allocate `Box::new(OverrideContext { db_handle: db, is_file_backed })`.
    - Update the doc-comment to reference Phase 65 RESEARCH §6 rationale.
    - `sv_drop_override_context` requires no behavioural change beyond ensuring it still type-checks (it operates on `*mut c_void`).

    Step 3 — Update all catalog-read call sites inside parse.rs. Per RESEARCH §5 + PATTERNS, the sites are:
    - `rewrite_to_native_sql` (~`src/parse.rs:1715-1768`) — catalog existence / lookup
    - `rewrite_drop_or_alter` (~`src/parse.rs:1770-1815`) — catalog existence / lookup
    - `rewrite_create` (~`src/parse.rs:1820-1935`) — catalog existence (for `IF NOT EXISTS`) + lookup
    - Enrichment block using `ctx.catalog.raw()` to call `execute_sql_raw` (~`src/parse.rs:1989-2035`)
    - Any other site found by `grep -n "ctx.catalog\|ctx\.catalog" src/parse.rs`

    At each site, replace the existing `ctx.catalog.<method>` call with:
    1. `let guard = unsafe { crate::conn_guard::ConnGuard::open(ctx.db_handle) }.map_err(<map to local Result error type>)?;`
    2. `let catalog = crate::catalog::CatalogReader::new(guard.raw(), /*catalog_table_present=*/ true);`
    3. The original `catalog.<method>(...)` call, unchanged.
    4. For raw-API reads (`execute_sql_raw(ctx.catalog.raw(), ...)`), substitute `guard.raw()`.
    5. Let the `guard` drop naturally at scope end.

    For the `catalog_table_present` flag: today it is captured at `init_extension` time and embedded inside `CatalogReader`.
    The flag is still relevant — it controls whether `lookup`/`list_all` should run their queries or return `Ok(None)`/empty.
    Stash it on `OverrideContext` too: change the struct to also carry `catalog_table_present: bool` (i.e., 3 fields total:
    `db_handle`, `catalog_table_present`, `is_file_backed`). `sv_make_override_context` then takes
    `(db, catalog_table_present, is_file_backed)`. Update the C++ shim signature in lockstep (Task 2 of this plan).

    Step 4 — Add a Rust unit test demonstrating the struct's new shape (per VALIDATION B13):
    - Inside `#[cfg(test)] mod tests`, add `fn override_context_carries_db_handle_not_conn()` that constructs an
      `OverrideContext { db_handle: std::ptr::null_mut(), catalog_table_present: false, is_file_backed: false }` and asserts
      via `std::mem::size_of` that no `CatalogReader` field is present. Alternative grep-style test: use `static_assertions`
      crate if present, or rely on the grep-audit in Plan 04.

    Step 5 — Phase 62 sqllogictest cross-check (informational, full re-run in Plan 04):
    - After the refactor, run `just build && just test-sql` against the existing transactional DDL suite locally. Capture
      output to `$TMPDIR/65_02_sqllogic.log`. The suite MUST stay green byte-identical (record any diff in summary).

    Do NOT modify `src/lib.rs`'s `init_extension` in this task beyond what is needed to call the updated
    `sv_register_parser_hooks` signature — leave the H2 `query_conn` removal for Plan 03. The `catalog_conn` local can stay
    in `init_extension` for now, but its consumers (the 14 read-side `register_table_function_with_extra_info` sites + 2
    scalars) are also Plan 03's job. The only `init_extension` change in this task is the `sv_register_parser_hooks` call —
    do that in Task 2.
  </action>
  <verify>
    <automated>cargo build --features extension --no-default-features 2>&1 | tail -40 && grep -E "db_handle: libduckdb_sys::duckdb_database|db_handle: ffi::duckdb_database" src/parse.rs && ! grep -E "INTENTIONAL LEAK.*Phase 62" src/parse.rs && grep -c "ConnGuard::open" src/parse.rs</automated>
  </verify>
  <acceptance_criteria>
    - `OverrideContext` struct contains `db_handle` (type `duckdb_database`), does NOT contain `catalog: CatalogReader` or
      `conn: duckdb_connection`. Verify with `grep -E "pub catalog:|pub conn:" src/parse.rs` returning no matches inside the
      `struct OverrideContext` block, AND `grep -E "db_handle: (libduckdb_sys::)?(ffi::)?duckdb_database" src/parse.rs` ≥1.
    - The "Phase 62 Q2 — INTENTIONAL LEAK" comment is removed. Verify with
      `grep -E "INTENTIONAL LEAK|Phase 62.*intentional" src/parse.rs` returning 0 matches.
    - `sv_make_override_context` signature accepts `duckdb_database` (and `catalog_table_present`), not `duckdb_connection`.
      Verify with `grep -A 4 "fn sv_make_override_context" src/parse.rs | grep -E "duckdb_database"` ≥1 match and
      `grep -A 4 "fn sv_make_override_context" src/parse.rs | grep -E "duckdb_connection"` 0 matches.
    - Every catalog read inside parse.rs uses `ConnGuard::open`. Verify with `grep -c "ConnGuard::open" src/parse.rs` ≥4
      (sites: rewrite_to_native_sql, rewrite_drop_or_alter, rewrite_create, enrichment — at minimum).
    - `cargo build --features extension --no-default-features` succeeds.
    - `cargo build` (bundled, default features) succeeds.
    - `cargo test --lib --features extension --no-default-features` succeeds with 0 failures. (Existing tests cover
      the parse.rs behaviour; this is the broad regression check at this layer.)
    - `just build && just test-sql -- v080_transactional_ddl` (or the closest invocation that targets that file) exits 0
      with no diffs against the prior expected output. Record evidence path in summary.
  </acceptance_criteria>
  <done>
    `OverrideContext` carries `db_handle`; every parser_override catalog read uses a per-call `ConnGuard`; Phase 62
    transactional DDL tests stay green. Plan 03 can now consume the new struct shape.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update C++ shim signature `sv_register_parser_hooks` + `init_extension` call site</name>
  <files>
    cpp/src/shim.cpp
    cpp/src/shim.hpp
    src/lib.rs
    src/parse.rs
  </files>
  <read_first>
    cpp/src/shim.cpp
    cpp/src/shim.hpp
    src/lib.rs
    src/parse.rs
    .planning/phases/65-overridecontext-connection-teardown/65-PATTERNS.md
  </read_first>
  <behavior>
    - `sv_register_parser_hooks` declared in `cpp/src/shim.hpp` and defined in `cpp/src/shim.cpp` no longer takes a
      `duckdb_connection` parameter. New signature: `bool sv_register_parser_hooks(duckdb_database db, bool catalog_table_present, bool is_file_backed)`.
    - The Rust extern declaration in `src/parse.rs` (or wherever the `sv_register_parser_hooks` `extern "C"` is declared)
      matches the new C++ signature.
    - `src/lib.rs::init_extension` invokes `sv_register_parser_hooks(db_handle, catalog_table_present, is_file_backed)` —
      no `catalog_conn` is threaded into the parser hook setup.
    - The shim internally calls `sv_make_override_context(db, catalog_table_present, is_file_backed)` (matching Task 1's
      updated signature) and stashes the returned Box pointer on `SemanticViewsParserInfo`.
    - Extension still loads cleanly; `just build` succeeds; `just test-sql` baseline tests stay green.
  </behavior>
  <action>
    Step 1 — Update `cpp/src/shim.hpp`:
    - Find the declaration of `sv_register_parser_hooks`. Replace `duckdb_connection catalog_conn` parameter with
      `bool catalog_table_present`. New full signature: `bool sv_register_parser_hooks(duckdb_database db, bool catalog_table_present, bool is_file_backed);`
    - Update the matching `extern "C"` block for `sv_make_override_context` so its parameter list reads
      `duckdb_database db, bool catalog_table_present, bool is_file_backed` (matching Task 1).

    Step 2 — Update `cpp/src/shim.cpp`:
    - In the body of `sv_register_parser_hooks`, drop any local use of `catalog_conn`. Pass the new triple
      `(db, catalog_table_present, is_file_backed)` through to `sv_make_override_context`.
    - Confirm `SemanticViewsParserInfo` continues to hold the resulting Box pointer; no other change to the C++ struct is
      required for this plan (the destruction-order semantics in `~SemanticViewsParserInfo` already only call
      `sv_drop_override_context`, which Box-frees the Rust-owned struct — that remains correct because the struct no longer
      owns a connection).
    - If there is any C++ code inside `cpp/src/shim.cpp` that called `duckdb_connect` to create the catalog conn (RESEARCH
      §5 H1 indicates it lives in `src/lib.rs:383-387` Rust-side, but verify the C++ side too), remove or short-circuit it.
    - Run `just build` to confirm the C++ amalgamation links cleanly.

    Step 3 — Update `src/parse.rs`'s `extern "C"` declaration for `sv_register_parser_hooks` (search for the `extern "C"` block):
    - Match the new C++ signature: `(db: duckdb_database, catalog_table_present: bool, is_file_backed: bool) -> bool`.

    Step 4 — Update `src/lib.rs::init_extension` (lines ~378-408 per RESEARCH §5 H1 site):
    - Replace the call `sv_register_parser_hooks(db_handle, catalog_conn, is_file_backed)` (or similar — verify exact arg order)
      with `sv_register_parser_hooks(db_handle, catalog_table_present, is_file_backed)`.
    - DO NOT yet remove the `catalog_conn` local — Plan 03 needs it intact for the 14 read-side registrations + 2 scalars
      that still consume `&catalog_reader`. The intent of Plan 02 is to surgically detach parser_override from the cached
      connection; the broader rewiring is Plan 03.
    - DO NOT yet remove `let query_conn = ...; let query_state = ...;` either — Plan 03 also covers those.

    Step 5 — Update `cpp/src/shim.cpp::~SemanticViewsParserInfo` (per RESEARCH §3.2 / Phase 62 §Q2): confirm it still calls
    `sv_drop_override_context(rust_state)` and nothing else connection-related. The destruction-order fix is automatic
    because there is no longer a connection to leak.

    Step 6 — Strip the dead "intentional leak" rationale from any related comments in `shim.cpp` / `shim.hpp` (search for
    `Phase 62 Q2`, `INTENTIONAL LEAK`, `bounded leak`). Replace with a one-line pointer: `// Phase 65: OverrideContext no longer owns a duckdb_connection. See .planning/phases/65/65-RESEARCH.md §3.3.`

    Step 7 — Build and run `just test-all` if quick (or at least `just build && just test-sql`) to confirm:
    (a) extension links and loads,
    (b) Phase 62 transactional DDL tests stay green byte-identical,
    (c) caret-rendering tests still pass.

    Save the test log to `$TMPDIR/65_02_t2_just_test_sql.log` for the summary.
  </action>
  <verify>
    <automated>just build 2>&1 | tee $TMPDIR/65_02_t2_build.log; tail -5 $TMPDIR/65_02_t2_build.log; grep -E "fn sv_register_parser_hooks|bool sv_register_parser_hooks" cpp/src/shim.cpp cpp/src/shim.hpp src/parse.rs src/lib.rs | grep -v "duckdb_connection"</automated>
  </verify>
  <acceptance_criteria>
    - `cpp/src/shim.hpp` declares `sv_register_parser_hooks(duckdb_database, bool, bool)`. Verify with
      `grep -E "sv_register_parser_hooks\s*\(\s*duckdb_database" cpp/src/shim.hpp` ≥1.
    - No remaining `duckdb_connection` parameter on `sv_register_parser_hooks` in any of shim.cpp, shim.hpp, parse.rs,
      lib.rs. Verify with
      `grep -E "sv_register_parser_hooks.*duckdb_connection" cpp/src/shim.cpp cpp/src/shim.hpp src/lib.rs src/parse.rs` returning 0 matches.
    - The `extern "C"` declaration of `sv_register_parser_hooks` in `src/parse.rs` matches the new signature. Verify by
      ensuring `cargo build --features extension --no-default-features` succeeds (the FFI type system enforces this).
    - `init_extension` calls `sv_register_parser_hooks(db_handle, catalog_table_present, is_file_backed)` (no `catalog_conn`
      argument). Verify with `grep -A 1 "sv_register_parser_hooks(" src/lib.rs | grep -v "//" | grep -E "catalog_table_present|catalog_conn"` showing `catalog_table_present` present and `catalog_conn` absent in the call.
    - `just build` succeeds (extension binary produced).
    - `just test-sql` exits 0 (Phase 62 transactional DDL + all other sqllogictests). Evidence path in summary.
    - "INTENTIONAL LEAK" / "Phase 62 Q2" leak comments removed from `cpp/src/shim.cpp`, `cpp/src/shim.hpp`.
      Verify with `grep -E "INTENTIONAL LEAK|bounded leak" cpp/src/` returning 0 matches.
  </acceptance_criteria>
  <done>
    C++ shim and Rust callers now thread `db_handle` (not `duckdb_connection`) through `sv_register_parser_hooks`. The
    parser_override path is fully decoupled from the long-lived `catalog_conn`. Phase 62 transactional DDL coverage stays
    byte-identical green. Plan 03 takes over the H2 `query_conn` removal and the 14+2 read-side rewiring.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust ↔ C++ shim | `sv_register_parser_hooks` and `sv_make_override_context` change ABI; both sides must update in lockstep |
| Parser_override invocation context | New `duckdb_connect` per parse-callback fire — RESEARCH §A7 marks re-entrancy MEDIUM until Wave 0 spike confirms |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-04 | Tampering | ABI mismatch between C++ shim and Rust extern decl | mitigate | Both sides updated in same task; `cargo build --features extension` linker catches mismatch immediately; `just build` re-run confirms |
| T-65-05 | Denial of Service | Per-DDL `duckdb_connect`/`duckdb_disconnect` deadlock if A7 was wrong | mitigate | Plan 01 spike A7 must record `RE-ENTRANCY-SAFE` before this plan starts; if spike was `DEFERRED-TO-PLAN-02`, Task 1's first build+test cycle surfaces the deadlock immediately and triggers a stop-and-revisit |
| T-65-06 | Repudiation | None — phase is internal lifecycle, no audit log impact | accept | n/a |
</threat_model>

<verification>
After both tasks complete:

1. `cargo build` and `cargo build --features extension --no-default-features` both succeed.
2. `just build` produces the extension binary.
3. `just test-sql` exits 0 (Phase 62 transactional DDL + caret + all other sqllogictests stay green byte-identical).
4. `grep -E "INTENTIONAL LEAK|Phase 62.*intentional" src/parse.rs cpp/src/shim.cpp cpp/src/shim.hpp` returns 0 matches.
5. `grep -E "sv_register_parser_hooks.*duckdb_connection" src/ cpp/` returns 0 matches.
6. `grep -c "ConnGuard::open" src/parse.rs` ≥ 4 (minimum sites listed in PATTERNS).
7. The H1 connection (`catalog_conn` opened in `init_extension`) is NO LONGER referenced inside the parser_override path,
   but its consumers in `init_extension` for the 14 read-side table function registrations + 2 scalar functions still
   compile and link. (That ownership moves in Plan 03.)
</verification>

<success_criteria>
- `OverrideContext` carries `db_handle: duckdb_database` (and `catalog_table_present`, `is_file_backed`); no connection field.
- `Drop for OverrideContext` removed; Phase 62 leak comment removed.
- Every parser_override catalog-read site uses a per-call `ConnGuard`.
- C++ shim's `sv_register_parser_hooks` signature updated; both sides build cleanly.
- Phase 62 transactional DDL + caret + all sqllogictests stay green byte-identical.
</success_criteria>

<output>
Create `.planning/phases/65-overridecontext-connection-teardown/65-02-SUMMARY.md` when done. Summary MUST include:
- Final OverrideContext field list and Drop status (none / no-op).
- New `sv_register_parser_hooks` C++ signature verbatim.
- Path to the `just test-sql` log proving Phase 62 byte-identical greenness.
- Count of `ConnGuard::open` call sites added in `src/parse.rs`.
- Any deviation from PATTERNS.md prescribed shape.
</output>
